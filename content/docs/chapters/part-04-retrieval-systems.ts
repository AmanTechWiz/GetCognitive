import type { DocPage } from '../pages';

export const part04RetrievalSystemsPages: DocPage[] = [
  {
    slug: ['part-iv', 'chapter-12-retrieval-augmented-generation'],
    title: 'Chapter 12 - Retrieval-Augmented Generation (RAG)',
    description:
      'Build a working mental model of RAG, then implement an end-to-end pipeline: chunking, embeddings, vector search, hybrid search, and context packing.',
    group: 'Part IV - Retrieval Systems',
    toc: [
      'The Big Idea',
      'RAG Architecture',
      'A Minimal End-to-End RAG (Tutorial)',
      'Document Chunking',
      'Embeddings',
      'Vector Databases (And What They Actually Do)',
      'Metadata Filtering',
      'Hybrid Search',
      'Context Packing (Getting The Most From Your Tokens)',
      'Freshness And Updates',
      'Production Tradeoffs And Failure Modes',
      'Exercises',
      'Summary',
    ],
    body: "# Chapter 12 - Retrieval-Augmented Generation (RAG)\n\nRAG is how you teach an LLM to answer questions using *your* documents, without retraining the model.\n\nIf you remember one sentence from this chapter, make it this:\n\n> RAG is a pipeline that turns a user question into: (1) a search query, (2) a small set of relevant passages, and (3) an answer that is *grounded* in those passages.\n\nThis chapter is an end-to-end tutorial. You will build a minimal RAG system (in pseudocode/TypeScript-style code), then you will learn what breaks in production and how to fix it.\n\n## The Big Idea\n\nAn LLM is good at:\n\n- Writing fluent text\n- Following instructions\n- Combining information that is already in its context window\n\nAn LLM is not good at:\n\n- Knowing your private docs by default\n- Keeping up with changing information\n- Citing sources reliably unless you design the workflow for it\n\nRAG solves this by retrieving relevant information and *placing it in the prompt*.\n\n### A Simple Diagram\n\n\\`\\`\\`text\nUser question\n   |\n   v\nRetriever (search in your knowledge base)\n   |\n   v\nTop-k passages + metadata (titles, URLs, timestamps, permissions)\n   |\n   v\nPrompt builder (packs passages into context)\n   |\n   v\nLLM (generate grounded answer)\n   |\n   v\nAnswer + citations (and optionally: JSON, actions, tool calls)\n\\`\\`\\`\n\nRAG is not magic. It is a set of engineering choices:\n\n- How you chunk documents\n- How you embed text\n- How you store vectors\n- How you rank results\n- How you pack context\n- How you handle updates and permissions\n- How you evaluate and debug\n\n## RAG Architecture\n\nThink of RAG as two pipelines:\n\n1. **Ingestion pipeline** (offline or background): documents -> chunks -> embeddings -> index\n2. **Query pipeline** (online / request-time): question -> search -> passages -> prompt -> answer\n\n\\`\\`\\`text\n           INGESTION (slow, batch)                     QUERY (fast, online)\n\n Raw docs (PDF/HTML/MD)                          User question\n          |                                             |\n          v                                             v\n  Parse + clean text                             Normalize question\n          |                                             |\n          v                                             v\n   Chunk into passages                           Embed question\n          |                                             |\n          v                                             v\n  Embed each chunk (vector)               Vector search / hybrid search\n          |                                             |\n          v                                             v\n Store: vector + text + metadata              Rerank + filter + dedupe\n          |                                             |\n          v                                             v\n     Build index                                Pack context + instruct LLM\n                                                      |\n                                                      v\n                                              Answer + citations\n\\`\\`\\`\n\nWhen students struggle with RAG, it is usually because they mix these two pipelines in their head.\n\n- In ingestion, you want correctness and stable indexing.\n- In query-time retrieval, you want relevance, low latency, and tight context.\n\n## A Minimal End-to-End RAG (Tutorial)\n\nWe will build a RAG system in four layers:\n\n1. Data model (chunks + metadata)\n2. Chunking\n3. Embeddings + storage\n4. Retrieval + prompt packing + answering\n\nThis is intentionally vendor-agnostic. You can plug in any embedding model and any vector store.\n\n### Step 0: Pick A Small Knowledge Base\n\nStart with 10-50 documents you can reason about.\n\n- Team wiki pages\n- Product docs\n- Lecture notes\n- A handful of PDFs\n\nDo not start with 100,000 files. You want to debug quality before you scale.\n\n### Step 1: Define Your Chunk Object\n\n\\`\\`\\`ts\ntype Chunk = {\n  id: string;\n  docId: string;\n  title: string;\n  url?: string;\n  text: string;\n  // For debugging and better packing:\n  startChar?: number;\n  endChar?: number;\n  // For filtering and safety:\n  metadata: {\n    source: 'wiki' | 'pdf' | 'github' | 'notion' | 'zendesk' | string;\n    createdAt?: string; // ISO date\n    updatedAt?: string; // ISO date\n    tags?: string[];\n    // Permissions are critical in production.\n    // Keep it simple here: a list of allowed user group ids.\n    allowedGroups?: string[];\n  };\n};\n\\`\\`\\`\n\nMake sure you store both:\n\n- the vector embedding\n- the raw text\n- enough metadata to debug and filter\n\n### Step 2: Chunk The Documents\n\nChunking is where RAG quality is often won or lost.\n\nChunking goals:\n\n- Each chunk should contain a coherent idea that can answer some question.\n- Each chunk should be small enough to fit several of them in the prompt.\n- Each chunk should include enough local context to stand alone.\n\nOne practical baseline: **chunk by tokens with overlap**, but respect structure (headings, paragraphs) when possible.\n\n\\`\\`\\`ts\n// A simple chunker: split by paragraphs, then merge until target size.\n// This is a baseline, not the final boss.\nexport function chunkText(input: {\n  docId: string;\n  title: string;\n  url?: string;\n  text: string;\n  targetChars?: number;\n  overlapChars?: number;\n  metadata: Chunk['metadata'];\n}): Chunk[] {\n  const targetChars = input.targetChars ?? 1200; // ~200-300 tokens depending on language\n  const overlapChars = input.overlapChars ?? 200;\n\n  const paras = input.text\n    .split(/\\\\n\\\\s*\\\\n+/)\n    .map((p) => p.trim())\n    .filter(Boolean);\n\n  const chunks: Chunk[] = [];\n  let buffer = '';\n  let cursor = 0;\n\n  function pushChunk(text: string, startChar: number, endChar: number) {\n    const id = \\`\\${input.docId}:\\${startChar}-\\${endChar}\\`;\n    chunks.push({\n      id,\n      docId: input.docId,\n      title: input.title,\n      url: input.url,\n      text,\n      startChar,\n      endChar,\n      metadata: input.metadata,\n    });\n  }\n\n  let startChar = 0;\n  for (const p of paras) {\n    const candidate = buffer ? buffer + '\\\\n\\\\n' + p : p;\n    if (candidate.length <= targetChars) {\n      buffer = candidate;\n      continue;\n    }\n\n    // Flush buffer as a chunk.\n    if (buffer) {\n      const endChar = startChar + buffer.length;\n      pushChunk(buffer, startChar, endChar);\n\n      // Overlap: keep the last overlapChars from the buffer.\n      const tail = buffer.slice(Math.max(0, buffer.length - overlapChars));\n      startChar = endChar - tail.length;\n      buffer = tail + '\\\\n\\\\n' + p;\n    } else {\n      // Single paragraph longer than target: hard-split.\n      for (let i = 0; i < p.length; i += targetChars - overlapChars) {\n        const piece = p.slice(i, i + targetChars);\n        const s = cursor + i;\n        const e = s + piece.length;\n        pushChunk(piece, s, e);\n      }\n      cursor += p.length;\n      startChar = cursor;\n      buffer = '';\n    }\n  }\n\n  if (buffer) {\n    const endChar = startChar + buffer.length;\n    pushChunk(buffer, startChar, endChar);\n  }\n\n  return chunks;\n}\n\\`\\`\\`\n\nThis chunker is not perfect, but it gives you a working system quickly.\n\n### Step 3: Embed And Store\n\nYou need an embedding function:\n\n\\`\\`\\`ts\ntype Embedder = {\n  embed(texts: string[]): Promise<number[][]>; // each embedding is an array of numbers\n  dims: number;\n};\n\\`\\`\\`\n\nAnd a store:\n\n\\`\\`\\`ts\ntype VectorRecord = {\n  id: string;\n  vector: number[];\n  chunk: Chunk;\n};\n\ntype VectorStore = {\n  upsert(records: VectorRecord[]): Promise<void>;\n  search(queryVector: number[], options: {\n    k: number;\n    filter?: (chunk: Chunk) => boolean;\n  }): Promise<{ score: number; chunk: Chunk }[]>;\n};\n\\`\\`\\`\n\nIn production you will use a real vector database or Postgres with pgvector. For learning, you can start with an in-memory store (slow but simple):\n\n\\`\\`\\`ts\nfunction dot(a: number[], b: number[]) {\n  let s = 0;\n  for (let i = 0; i < a.length; i++) s += a[i] * b[i];\n  return s;\n}\n\nfunction l2norm(v: number[]) {\n  return Math.sqrt(dot(v, v));\n}\n\nfunction cosineSimilarity(a: number[], b: number[]) {\n  const denom = l2norm(a) * l2norm(b);\n  if (denom === 0) return 0;\n  return dot(a, b) / denom;\n}\n\nexport function createInMemoryVectorStore(): VectorStore {\n  const records = new Map<string, VectorRecord>();\n\n  return {\n    async upsert(rs) {\n      for (const r of rs) records.set(r.id, r);\n    },\n    async search(q, { k, filter }) {\n      const scored: { score: number; chunk: Chunk }[] = [];\n      for (const r of records.values()) {\n        if (filter && !filter(r.chunk)) continue;\n        const score = cosineSimilarity(q, r.vector);\n        scored.push({ score, chunk: r.chunk });\n      }\n      scored.sort((a, b) => b.score - a.score);\n      return scored.slice(0, k);\n    },\n  };\n}\n\\`\\`\\`\n\nNow ingest:\n\n\\`\\`\\`ts\nexport async function ingestDocs(input: {\n  docs: { docId: string; title: string; url?: string; text: string; metadata: Chunk['metadata'] }[];\n  embedder: Embedder;\n  store: VectorStore;\n}) {\n  const allChunks: Chunk[] = [];\n  for (const d of input.docs) {\n    allChunks.push(\n      ...chunkText({\n        docId: d.docId,\n        title: d.title,\n        url: d.url,\n        text: d.text,\n        metadata: d.metadata,\n      }),\n    );\n  }\n\n  // Embed in batches\n  const batchSize = 64;\n  for (let i = 0; i < allChunks.length; i += batchSize) {\n    const batch = allChunks.slice(i, i + batchSize);\n    const vectors = await input.embedder.embed(batch.map((c) => c.text));\n    await input.store.upsert(\n      batch.map((chunk, j) => ({\n        id: chunk.id,\n        vector: vectors[j],\n        chunk,\n      })),\n    );\n  }\n}\n\\`\\`\\`\n\n### Step 4: Retrieve, Pack, Answer\n\nRetrieval:\n\n\\`\\`\\`ts\nexport async function retrieveTopK(input: {\n  question: string;\n  k: number;\n  embedder: Embedder;\n  store: VectorStore;\n  allowedGroups?: string[];\n}) {\n  const [qVec] = await input.embedder.embed([input.question]);\n\n  return input.store.search(qVec, {\n    k: input.k,\n    filter: (chunk) => {\n      const acl = chunk.metadata.allowedGroups;\n      if (!acl || acl.length === 0) return true;\n      const userGroups = input.allowedGroups ?? [];\n      return acl.some((g) => userGroups.includes(g));\n    },\n  });\n}\n\\`\\`\\`\n\nPacking:\n\n\\`\\`\\`ts\nexport function packContext(results: { score: number; chunk: Chunk }[], options?: {\n  maxChars?: number;\n}): { context: string; sources: { title: string; url?: string }[] } {\n  const maxChars = options?.maxChars ?? 8000;\n\n  let context = '';\n  const sources: { title: string; url?: string }[] = [];\n\n  for (const r of results) {\n    const header = \\`[Source] \\${r.chunk.title}\\${r.chunk.url ? \\` (\\${r.chunk.url})\\` : ''}\\\\n\\`;\n    const block = header + r.chunk.text.trim() + '\\\\n\\\\n';\n    if ((context + block).length > maxChars) break;\n    context += block;\n    sources.push({ title: r.chunk.title, url: r.chunk.url });\n  }\n\n  return { context: context.trim(), sources };\n}\n\\`\\`\\`\n\nAnswering (LLM call abstracted):\n\n\\`\\`\\`ts\ntype LLM = {\n  complete(prompt: string): Promise<string>;\n};\n\nexport async function answerWithRag(input: {\n  question: string;\n  results: { score: number; chunk: Chunk }[];\n  llm: LLM;\n}) {\n  const { context } = packContext(input.results);\n\n  const prompt = [\n    'You are a helpful assistant.',\n    'Use ONLY the provided sources to answer. If the sources do not contain the answer, say so.',\n    'Cite sources by title in parentheses.',\n    '',\n    'SOURCES:',\n    context || '(no sources found)',\n    '',\n    'QUESTION:',\n    input.question,\n    '',\n    'ANSWER:',\n  ].join('\\\\n');\n\n  return input.llm.complete(prompt);\n}\n\\`\\`\\`\n\nThat is a minimal RAG system. It will produce answers. Now we make it *good*.\n\n## Document Chunking\n\nChunking is a compression problem:\n\n- Too small: retrieval finds fragments that lack the key detail.\n- Too large: retrieval returns bulky chunks that waste context and contain distractions.\n\n### Chunk Size: A Student-Friendly Rule Of Thumb\n\nStart with:\n\n- 150-350 tokens per chunk\n- 10-20% overlap\n\nThen measure:\n\n- Are the top results missing definitions because the definition is in the previous chunk?\n- Are chunks mixing unrelated sections because they are too large?\n\n### Chunking Strategies (Pick A Baseline)\n\n1. **Fixed-size token chunks**: simplest, robust across formats.\n2. **Structure-aware chunks**: split by headings, list items, tables; better for docs.\n3. **Semantic chunks**: split when topic shifts; helpful but harder to implement reliably.\n4. **Windowed chunks**: store sentence windows, then expand around hits at query time.\n\n### Why Overlap Exists\n\nOverlap is a band-aid for boundary problems:\n\n\\`\\`\\`text\nChunk A: \"... the system uses metadata filters to ...\"\nChunk B: \"... enforce permissions and prevent data leaks.\"\n\nQuestion: \"How do metadata filters prevent leaks?\"\n\nIf A and B do not overlap, either chunk alone might be incomplete.\n\\`\\`\\`\n\nOverlap increases recall, but it also increases index size and near-duplicates.\n\n### Common Chunking Mistakes\n\n- Chunking raw PDFs without cleaning line breaks (retrieval becomes noisy).\n- Chunking code without keeping function signatures and docstrings together.\n- Chunking with no metadata (debugging becomes guesswork).\n- Chunking multi-column PDFs as a single text stream (sentences interleave).\n\n### A Practical Upgrade: Add A Header To Every Chunk\n\nWhen your docs have structure, include it in the chunk text *before embedding*:\n\n\\`\\`\\`text\nTitle: Incident Response Runbook\nSection: 3.2 Rotating API Keys\nBody: ...\n\\`\\`\\`\n\nThis makes embeddings more meaningful because the chunk carries topical cues.\n\n## Embeddings\n\nEmbeddings map text to vectors so that \"similar meaning\" ends up \"nearby\" in vector space.\n\n### A Mental Model\n\nImagine a huge space where each direction is a concept, and each text becomes a point:\n\n\\`\\`\\`text\n           (concept: \"billing\")\n                 ^\n                 |\n                 |\n (shipping) <----+----> (security)\n                 |\n                 v\n           (concept: \"auth\")\n\\`\\`\\`\n\nVector similarity (often cosine similarity) approximates semantic similarity.\n\n### What You Actually Control\n\nWhen you choose an embedding model, you control:\n\n- Dimensionality (vector length)\n- Cost per token or per request (hosted embeddings)\n- Latency (network + model compute)\n- Domain fit (general vs code vs multilingual)\n- Whether you can run it on-device/on-prem\n\n### Normalize Text Before Embedding\n\nDo not embed garbage.\n\nDo:\n\n- Remove boilerplate nav, cookie banners, repeated headers/footers\n- Convert tables into a linear but readable format\n- Preserve key-value structure (config docs, API references)\n\nDo not:\n\n- Strip punctuation blindly (you destroy meaning in code and citations)\n- Collapse all whitespace into one space (you lose list/table cues)\n\n### Store The Embedding Inputs For Debugging\n\nWhen retrieval fails, you want to answer:\n\n- What exact chunk text did we embed?\n- Was it truncated?\n- Did we include the section header?\n- Did the parser accidentally drop a paragraph?\n\nSo store the exact chunk text in your DB and log the chunk id in traces.\n\n## Vector Databases (And What They Actually Do)\n\nA vector DB is not magic either. It is:\n\n- A place to store vectors and metadata\n- A fast approximate nearest neighbor (ANN) index\n- A query engine with filtering and sometimes hybrid search\n\n### Why Approximate Search?\n\nExact nearest neighbor search is too slow at scale.\n\nANN indexes trade a tiny amount of accuracy for huge speedups.\n\n### What You Should Look For\n\n- Filtering performance (metadata filters must not destroy latency)\n- Index build time and update behavior (do inserts require reindexing?)\n- Recall vs latency knobs (can you tune it?)\n- Multi-tenancy and isolation (prod requirement)\n- Operational maturity (backups, monitoring, incident tools)\n\n### Debugging Tip: Always Log The Top Results\n\nFor each request, log:\n\n- query text\n- top-k chunk ids + scores\n- titles/urls\n- whether filters were applied\n- total retrieval time\n\nIf you do not log retrieval results, you cannot debug RAG.\n\n## Metadata Filtering\n\nMetadata filtering is how you keep retrieval relevant and safe.\n\nExamples:\n\n- **Permissions**: only retrieve chunks the user is allowed to see.\n- **Freshness**: prefer chunks updated recently.\n- **Product area**: limit to the correct domain (billing vs auth).\n- **Language**: match the user language.\n\n### The Permissions Trap (Very Important)\n\nIf you retrieve from a shared index without access control, you can leak private data.\n\nTwo common architectures:\n\n1. **Filter at query time**: store ACL metadata and filter in vector search.\n2. **Separate indexes**: per-tenant or per-group indexes (simpler, more expensive).\n\nIf you do query-time filters, test them like you test authentication.\n\n## Hybrid Search\n\nVector search is great for meaning. Keyword search (BM25) is great for:\n\n- Exact terms (error codes, IDs, function names)\n- Rare proper nouns\n- Very short queries\n\nHybrid search combines them.\n\n\\`\\`\\`text\nScore = alpha * vector_similarity + (1 - alpha) * keyword_score\n\\`\\`\\`\n\n### When Hybrid Helps A Lot\n\n- \"What does error E042 mean?\"\n- \"How do I set env var FOO_BAR in docker compose?\"\n- \"Where is function validateJwt used?\"\n\nPure vector search might miss exact matches. Pure keyword search might miss paraphrases.\n\n### A Practical Hybrid Pattern\n\n1. Retrieve top 50 with BM25\n2. Retrieve top 50 with vectors\n3. Union the sets (dedupe)\n4. Rerank to top 10 (next chapter covers reranking deeply)\n\n## Context Packing (Getting The Most From Your Tokens)\n\nOnce you have candidate chunks, you must decide:\n\n- Which chunks make it into the prompt?\n- In what order?\n- With how much metadata?\n\nPacking is an optimization problem with constraints.\n\n### Packing Failure Mode: The \"Distractor Chunk\"\n\nIf one retrieved chunk contains a plausible but wrong answer, the LLM may confidently use it.\n\nPacking mitigations:\n\n- Put the most relevant chunks first (and keep that ranking stable).\n- Dedupe near-duplicates to avoid overweighting one idea.\n- Include titles/URLs so the model can separate sources.\n- Provide clear instructions: use sources only, cite them, say \"not found\" otherwise.\n\n### A Better Context Format\n\nInstead of raw concatenation, use a consistent format:\n\n\\`\\`\\`text\n[1] Title: ...\n    URL: ...\n    Updated: 2026-01-14\n    Excerpt:\n    \"...\"\n\n[2] Title: ...\n...\n\\`\\`\\`\n\nThis makes it easier for the LLM to cite and cross-check.\n\n### Context Packing Heuristics That Actually Work\n\n- Cap per-source tokens so one long doc does not dominate.\n- Mix breadth and depth: include 1-2 top chunks from several documents before adding more from the same doc.\n- Prefer chunks with \"definition-like\" patterns for \"what is X\" queries.\n- Prefer chunks with code blocks for \"how do I implement X\" queries.\n\n## Freshness And Updates\n\nRAG is often chosen because you need up-to-date info.\n\nFreshness problems happen when:\n\n- The source of truth changes but your index is stale\n- There are multiple versions (v1, v2 docs) and you retrieve both\n- Old policies remain in the index and are still relevant to some queries\n\n### Build An Update Loop\n\nAt minimum, you need:\n\n- A way to detect changes (webhook, polling, git commits, last-modified timestamps)\n- A way to re-chunk and re-embed changed documents\n- A way to delete old chunks (or mark them inactive)\n\nIf you cannot delete old chunks, you will accumulate stale answers.\n\n### Staleness Mitigation Patterns\n\n- Store `updatedAt` and boost recent chunks\n- Separate indexes by version (docs-v1, docs-v2)\n- Add a \"current policy\" label and filter by it\n\n## Production Tradeoffs And Failure Modes\n\n### Latency vs Quality\n\nEvery retrieval improvement costs something:\n\n- Larger `k` improves recall but increases reranking cost and context packing complexity.\n- Hybrid search improves robustness but adds query-time work.\n- Cross-encoder reranking is high quality but can be expensive.\n\nA common production compromise:\n\n\\`\\`\\`text\nFast path: vector search only (k=8) -> answer\nSlow path: hybrid + rerank (k=50) -> answer\nTrigger slow path when confidence is low or user asks for citations.\n\\`\\`\\`\n\n### Cost Drivers\n\n- Embeddings for ingestion (one-time, but can be large)\n- Embeddings for queries (per request)\n- Reranker model compute (per request)\n- Prompt tokens for context (per request)\n\n### Failure Modes Checklist\n\n1. **Parser failure**: the text you indexed is wrong.\n2. **Chunking failure**: information split across chunks in a way that hurts retrieval.\n3. **Embedding mismatch**: domain/language mismatch lowers similarity quality.\n4. **Retrieval miss**: top-k does not include the right chunk.\n5. **Packing failure**: right chunk retrieved but not included due to token limits or bad heuristics.\n6. **Generation failure**: sources are correct but the LLM misreads or fabricates.\n7. **Permission failure**: user sees content they should not.\n\nTreat these as separate bugs. Different fixes apply to each.\n\n## Exercises\n\n1. **Chunking experiment**\n\nTake one long document and build three chunkers:\n\n- 200 token chunks with 20 token overlap\n- 400 token chunks with 40 token overlap\n- Heading-based chunks (split on Markdown headings)\n\nCreate 20 questions and compare which chunker retrieves the right passage most often.\n\n2. **Hybrid vs vector**\n\nCreate 10 questions that include exact terms (error codes, function names, config keys). Compare:\n\n- vector-only retrieval\n- keyword-only retrieval\n- hybrid union + rerank (simple rerank: prefer chunks that contain the exact term)\n\n3. **Freshness drill**\n\nModify a document so that a key detail changes (for example: a policy threshold). Update your index and confirm retrieval returns the new version. Then test what happens if you forget to delete old chunks.\n\n4. **Permissions drill**\n\nCreate two documents:\n\n- one public\n- one restricted (allowedGroups = ['admin'])\n\nVerify that users without the group never retrieve restricted chunks, even if the restricted chunk is the best semantic match.\n\n## Summary\n\nRAG is a pipeline, not a feature.\n\n- Ingestion: parse -> chunk -> embed -> index\n- Query: embed -> retrieve -> filter -> pack -> generate\n\nQuality comes from careful chunking, solid metadata, and disciplined context packing. Production safety depends on permissions-aware retrieval and a reliable update loop.",
  },
  {
    slug: ['part-iv', 'chapter-13-advanced-retrieval'],
    title: 'Chapter 13 - Advanced Retrieval',
    description:
      'Make retrieval robust: rerankers, query rewriting, multi-hop retrieval, graph-based retrieval, long-context strategies, and agentic retrieval patterns.',
    group: 'Part IV - Retrieval Systems',
    toc: [
      'Why Advanced Retrieval Exists',
      'Reranking (Cross-Encoders)',
      'A Practical Hybrid + Rerank Pipeline (Tutorial)',
      'Deduping And Diversity (MMR)',
      'Context Compression',
      'Multi-Hop Retrieval',
      'Graph RAG',
      'Long Context Retrieval Strategies',
      'Knowledge Graphs In Practice',
      'Agentic Retrieval',
      'Production Tradeoffs',
      'Failure Patterns And Fixes',
      'Exercises',
      'Summary',
    ],
    body: "# Chapter 13 - Advanced Retrieval\n\nChapter 12 got you to a working baseline RAG system. Baseline RAG is valuable, but it fails in predictable ways:\n\n- The right chunk exists, but retrieval does not rank it in the top-k.\n- Queries are short, ambiguous, or use different wording than the docs.\n- The index contains near-duplicates and distractors.\n- The user question requires combining multiple facts from different places.\n\nAdvanced retrieval is the set of techniques that push you from \"sometimes works\" to \"reliable enough to ship\".\n\n## Why Advanced Retrieval Exists\n\nVector similarity is a blunt instrument.\n\nIt answers: \"Which chunks are semantically near this question?\"\n\nBut relevance is more specific:\n\n- Does this chunk *answer* the question?\n- Is it the latest version?\n- Is it about the correct product / tenant / region?\n- Does it match the user's intent (how-to vs definition vs policy)?\n\nThe core idea of advanced retrieval:\n\n> Use a fast first-stage retriever to find candidates, then use stronger (but slower) models and heuristics to select the best evidence.\n\n## Reranking (Cross-Encoders)\n\n### Bi-Encoder vs Cross-Encoder (Student Mental Model)\n\n**Bi-encoder** retrieval (what you did with embeddings):\n\n- Embed question -> vector\n- Embed chunks -> vectors (precomputed)\n- Compare vectors fast\n\nGood: fast at scale.\n\nBad: similarity is approximate; it can miss fine-grained relevance.\n\n**Cross-encoder** reranking:\n\n- For each candidate chunk, feed \\`[question, chunk]\\` to a model\n- The model outputs a relevance score\n\nGood: much better ranking quality.\n\nBad: compute cost grows with number of candidates.\n\n\\`\\`\\`text\nFirst stage:  q -> vector search -> 50 candidates (fast)\nSecond stage: cross-encoder ranks 50 pairs (slower, but accurate)\nFinal: top 5-10 chunks for context\n\\`\\`\\`\n\n### When Reranking Helps The Most\n\n- Many similar chunks (lots of near-duplicates)\n- Questions that require exact constraints (\"only when X and Y are true\")\n- Queries that mix multiple topics (model needs to focus on the right subpart)\n\n### Reranking Without A Cross-Encoder (Cheap Baseline)\n\nIf you cannot afford a cross-encoder, you can still rerank using heuristics:\n\n- boost chunks where key terms from the query appear verbatim\n- boost chunks from more authoritative sources (handbook vs chat logs)\n- boost recent chunks\n- penalize very short chunks (often headers without content)\n\nHeuristics are not as good, but they are cheap and often worth it.\n\n## A Practical Hybrid + Rerank Pipeline (Tutorial)\n\nWe will implement a retrieval pipeline that is common in production:\n\n1. Candidate retrieval: vector + keyword\n2. Merge and dedupe\n3. Rerank\n4. Pack context with diversity\n\n### Step 1: Candidate Retrieval\n\n\\`\\`\\`ts\ntype Candidate = { chunk: Chunk; score: number; channel: 'vector' | 'keyword' };\n\ntype KeywordStore = {\n  searchKeywords(query: string, options: { k: number }): Promise<{ score: number; chunk: Chunk }[]>;\n};\n\nexport async function retrieveCandidates(input: {\n  question: string;\n  kVector: number;\n  kKeyword: number;\n  embedder: Embedder;\n  vectorStore: VectorStore;\n  keywordStore: KeywordStore;\n}) {\n  const [qVec] = await input.embedder.embed([input.question]);\n\n  const [vs, ks] = await Promise.all([\n    input.vectorStore.search(qVec, { k: input.kVector }),\n    input.keywordStore.searchKeywords(input.question, { k: input.kKeyword }),\n  ]);\n\n  const candidates: Candidate[] = [];\n  for (const r of vs) candidates.push({ chunk: r.chunk, score: r.score, channel: 'vector' });\n  for (const r of ks) candidates.push({ chunk: r.chunk, score: r.score, channel: 'keyword' });\n  return candidates;\n}\n\\`\\`\\`\n\n### Step 2: Merge + Dedupe\n\nNear-duplicates can overweight one idea and crowd out other evidence.\n\nStart simple: dedupe by \\`chunk.id\\`. Then consider deduping by hash of normalized text or by \\`docId + section\\` if you have it.\n\n\\`\\`\\`ts\nexport function dedupeCandidates(cands: Candidate[]) {\n  const byId = new Map<string, Candidate>();\n  for (const c of cands) {\n    const prev = byId.get(c.chunk.id);\n    if (!prev || c.score > prev.score) byId.set(c.chunk.id, c);\n  }\n  return [...byId.values()];\n}\n\\`\\`\\`\n\n### Step 3: Rerank\n\nWe will define a reranker interface so you can plug in a cross-encoder, an LLM, or heuristics.\n\n\\`\\`\\`ts\ntype Reranker = {\n  rerank(question: string, chunks: Chunk[]): Promise<{ chunk: Chunk; score: number }[]>;\n};\n\\`\\`\\`\n\nHeuristic reranker baseline:\n\n\\`\\`\\`ts\nfunction tokenize(q: string) {\n  return q\n    .toLowerCase()\n    .split(/[^a-z0-9_]+/)\n    .filter((t) => t.length >= 3);\n}\n\nexport function createHeuristicReranker(): Reranker {\n  return {\n    async rerank(question, chunks) {\n      const terms = new Set(tokenize(question));\n      return chunks\n        .map((chunk) => {\n          const text = chunk.text.toLowerCase();\n          let hits = 0;\n          for (const t of terms) if (text.includes(t)) hits += 1;\n          // Score: term hits + small boost for recency when available.\n          const recencyBoost = chunk.metadata.updatedAt ? 0.1 : 0;\n          return { chunk, score: hits + recencyBoost };\n        })\n        .sort((a, b) => b.score - a.score);\n    },\n  };\n}\n\\`\\`\\`\n\nThis is not \"state of the art\", but it is a useful stepping stone because it is explainable.\n\n### Step 4: Diversity In The Final Context (MMR)\n\nIf you pack the top 10 chunks by score, you often get 10 chunks saying the same thing.\n\nMMR (Maximum Marginal Relevance) is a classic approach:\n\n- pick a relevant chunk\n- then pick another chunk that is relevant but different\n- repeat\n\n\\`\\`\\`text\nGoal: maximize  lambda * relevance(chunk, query)\n               - (1-lambda) * similarity(chunk, already_selected)\n\\`\\`\\`\n\nPractical version (requires embeddings for chunks and query):\n\n\\`\\`\\`ts\ntype ChunkWithVec = { chunk: Chunk; vec: number[]; relScore: number };\n\nexport function mmrSelect(input: {\n  queryVec: number[];\n  candidates: ChunkWithVec[];\n  k: number;\n  lambda: number; // 0.0 = diversity only, 1.0 = relevance only\n}) {\n  const selected: ChunkWithVec[] = [];\n  const remaining = [...input.candidates];\n\n  while (selected.length < input.k && remaining.length > 0) {\n    let bestIdx = 0;\n    let bestScore = -Infinity;\n\n    for (let i = 0; i < remaining.length; i++) {\n      const c = remaining[i];\n      let diversityPenalty = 0;\n      for (const s of selected) {\n        diversityPenalty = Math.max(diversityPenalty, cosineSimilarity(c.vec, s.vec));\n      }\n      const score = input.lambda * c.relScore - (1 - input.lambda) * diversityPenalty;\n      if (score > bestScore) {\n        bestScore = score;\n        bestIdx = i;\n      }\n    }\n\n    selected.push(remaining.splice(bestIdx, 1)[0]);\n  }\n\n  return selected;\n}\n\\`\\`\\`\n\nIf you do not have vectors at this stage, you can approximate diversity by limiting chunks per docId:\n\n- at most 2 chunks per document in the final context (a very effective heuristic)\n\n## Context Compression\n\nSometimes retrieval finds the right document, but the chunk is too long or contains irrelevant details.\n\nContext compression means shrinking evidence before it enters the final prompt.\n\nThere are two broad approaches:\n\n1. **Extractive compression**: select the most relevant sentences/paragraphs from the chunk.\n2. **Abstractive compression**: summarize or rewrite the chunk into a shorter form (riskier for grounding).\n\n### Extractive Compression (Safer Baseline)\n\nSplit a chunk into sentences, then rank sentences against the question.\n\n\\`\\`\\`text\nChunk -> sentences -> embed sentences -> pick top N -> join\n\\`\\`\\`\n\nBenefits:\n\n- Keeps original wording\n- Lower risk of introducing hallucinated details\n\nCosts:\n\n- More compute at query time\n- Might break coherence if you pick isolated sentences\n\n### Abstractive Compression (Use With Guardrails)\n\nIf you use an LLM to compress evidence, constrain it:\n\n- Ask for bullet points that quote exact phrases\n- Ask it to keep numbers, dates, and conditions verbatim\n- Ask it to output \"cannot compress without losing constraints\" when needed\n\nIn production, you should evaluate compression carefully because it can silently remove critical details.\n\n## Multi-Hop Retrieval\n\nSome questions require multiple retrieval steps.\n\nExample:\n\n- Question: \"Can we store customer passports, and if so, what is the retention policy?\"\n\nThis might require:\n\n1. Retrieve policy about PII categories\n2. From that, discover passports are \"high sensitivity\"\n3. Retrieve retention policy for \"high sensitivity PII\"\n\n### A Simple Multi-Hop Pattern\n\n\\`\\`\\`text\nHop 1: retrieve -> extract missing subquestions -> retrieve again\n\\`\\`\\`\n\n\\`\\`\\`ts\ntype QueryPlanner = {\n  expand(question: string, evidence: Chunk[]): Promise<string[]>; // returns follow-up queries\n};\n\nexport async function multiHopRetrieve(input: {\n  question: string;\n  retrieve: (q: string) => Promise<{ score: number; chunk: Chunk }[]>;\n  planner: QueryPlanner;\n  hops: number;\n}) {\n  let queries = [input.question];\n  const all: { score: number; chunk: Chunk }[] = [];\n\n  for (let h = 0; h < input.hops; h++) {\n    const newEvidence: Chunk[] = [];\n    for (const q of queries) {\n      const results = await input.retrieve(q);\n      all.push(...results);\n      for (const r of results) newEvidence.push(r.chunk);\n    }\n\n    // Use planner to propose follow-up queries based on evidence.\n    queries = await input.planner.expand(input.question, newEvidence);\n    if (queries.length === 0) break;\n  }\n\n  return all;\n}\n\\`\\`\\`\n\nThis is powerful, but dangerous if it becomes an unbounded loop. Always cap hops and log the intermediate queries.\n\n## Graph RAG\n\nGraph RAG is a family of approaches where you build a graph of entities and relationships, then retrieve using graph structure.\n\nThink of your knowledge base as:\n\n\\`\\`\\`text\nNodes: people, services, policies, APIs, concepts\nEdges: depends-on, owned-by, requires, references, related-to\n\\`\\`\\`\n\nWhy this helps:\n\n- Multi-hop questions become graph traversals.\n- You can retrieve the right neighborhood even when exact wording differs.\n- You can reason about relationships explicitly.\n\n### A Practical Graph RAG Build\n\n1. Extract entities and relations from documents (LLM or rules).\n2. Create a graph store (even a simple adjacency list to start).\n3. When a query arrives:\n   - identify key entities in the query\n   - retrieve connected nodes and their supporting text\n   - optionally rerank supporting passages\n\n### A Text Diagram\n\n\\`\\`\\`text\nQuery: \"How does service A authenticate to service B?\"\n\nGraph neighborhood:\n  (Service A) --uses--> (OIDC) --issues--> (JWT) --validated-by--> (Service B)\n\nRetrieve supporting chunks for nodes:\n  - \"Service A obtains token via OIDC...\"\n  - \"Service B validates JWT using JWKS...\"\n\\`\\`\\`\n\nGraph RAG is a bigger investment. It shines when:\n\n- The domain is entity-heavy (large systems, many components)\n- Questions are multi-hop\n- Relationships matter more than exact text similarity\n\n## Long Context Retrieval Strategies\n\nIf your model has a very large context window, you might think retrieval is easier.\n\nReality:\n\n- Long context makes it *possible* to include more evidence, but you still need selection.\n- More context can add more distractors.\n\nStrategies:\n\n1. **Hierarchical retrieval**: retrieve documents, then sections, then chunks.\n2. **Progressive disclosure**: answer with minimal evidence, then expand if user asks.\n3. **Query-conditioned expansion**: retrieve a small chunk, then expand around it (previous/next chunks).\n\n### Query-Conditioned Expansion (Practical And Cheap)\n\n1. Retrieve a chunk id\n2. Fetch neighboring chunks from the same doc\n3. Pack as a contiguous excerpt\n\nThis reduces boundary problems while keeping retrieval precise.\n\n## Knowledge Graphs In Practice\n\nStudents often confuse:\n\n- \"Graph RAG\" (retrieval strategy using graph structure)\n- \"Knowledge graph\" (a data structure you maintain)\n\nYou can do Graph RAG without a full formal knowledge graph.\n\nStart with:\n\n- entity extraction for a few types (services, APIs, teams)\n- edges from explicit references (\"Service A calls /v1/login on Service B\")\n- store edges in a simple table\n\nThen iteratively expand.\n\n## Agentic Retrieval\n\nAgentic retrieval means the system can *decide* how to retrieve based on the question.\n\nExample decision tree:\n\n\\`\\`\\`text\nIf query contains an error code -> keyword search first\nElse if query is \"how to\" -> hybrid + rerank\nElse if query is multi-part -> multi-hop\nElse -> vector-only\n\\`\\`\\`\n\nAgentic retrieval works best when:\n\n- You constrain the toolset (retrieval actions are few and well-defined)\n- You log decisions and intermediate steps\n- You can evaluate the policy (Chapter 14)\n\n### A Minimal Retrieval Controller\n\n\\`\\`\\`ts\ntype RetrievalMode = 'vector' | 'hybrid_rerank' | 'multi_hop';\n\nexport function chooseRetrievalMode(question: string): RetrievalMode {\n  const q = question.toLowerCase();\n  if (/\\\\b(e\\\\d{2,}|0x[0-9a-f]+)\\\\b/.test(q)) return 'hybrid_rerank'; // error codes\n  if (q.includes('how do i') || q.includes('how to')) return 'hybrid_rerank';\n  if (q.includes('and') || q.includes('then') || q.includes('also')) return 'multi_hop';\n  return 'vector';\n}\n\\`\\`\\`\n\nThis is not \"AI\". It is just good engineering: pick the right tool for the job.\n\n## Production Tradeoffs\n\n### Latency Budgeting\n\nRough mental math for a single request:\n\n\\`\\`\\`text\nEmbedding query:      10-200ms (depends on provider and network)\nVector search:         5-50ms (depends on index + filters)\nKeyword search:        5-50ms\nReranking (50 pairs): 20-300ms (CPU/GPU model dependent)\nLLM generation:      200ms - several seconds\n\\`\\`\\`\n\nYou do not need perfect numbers. You need the habit:\n\n- measure each stage\n- keep a budget\n- make tradeoffs explicitly\n\n### Caching\n\nEffective caches in retrieval systems:\n\n- query embedding cache (for repeated queries)\n- retrieval results cache keyed by normalized query and filters\n- reranker cache for (query, chunkId) pairs\n\nBe careful: caching interacts with permissions and freshness.\n\n## Failure Patterns And Fixes\n\n1. **Reranker prefers wrong chunk**\n\nCause: candidates lack the right chunk; reranker cannot fix recall failures.\n\nFix: increase first-stage recall (bigger k, hybrid search, better chunking).\n\n2. **Multi-hop loops or drifts**\n\nCause: planner keeps generating new queries or follows tangents.\n\nFix: cap hops, require each hop to cite what it learned, and stop when confidence is high.\n\n3. **Compression drops constraints**\n\nCause: abstractive compression removes \"only if\" conditions or numbers.\n\nFix: prefer extractive compression; test with adversarial examples (Chapter 14).\n\n4. **Graph neighborhood too big**\n\nCause: common nodes connect to everything (like \"authentication\").\n\nFix: edge weighting, type constraints, or require explicit entity matches.\n\n## Exercises\n\n1. **Reranking ablation**\n\nTake 30 questions and compare:\n\n- vector-only top 8\n- hybrid union top 100 + heuristic rerank to top 8\n- hybrid union top 100 + cross-encoder rerank to top 8 (if available)\n\nMeasure: how often the right supporting chunk appears in the final context.\n\n2. **MMR vs top-k**\n\nPick 10 broad questions. Compare the packed contexts:\n\n- naive top 10 by reranker score\n- MMR-selected 10 with lambda=0.7\n\nJudge: does MMR include more distinct sources without losing correctness?\n\n3. **Multi-hop retrieval**\n\nDesign 10 questions that require two different documents. Implement a 2-hop retriever and log the intermediate queries. Identify failure cases where hop 2 becomes irrelevant.\n\n4. **Compression safety**\n\nCreate 10 chunks with \"sharp\" constraints (numbers, thresholds, conditions). Apply your compression method and check whether the constraints remain unchanged.\n\n## Summary\n\nAdvanced retrieval is about reliability:\n\n- Retrieve broadly (hybrid, higher k)\n- Then select precisely (reranking, diversity)\n- Then pack evidence efficiently (compression and expansion)\n\nUse these techniques to fix specific failure modes, and always measure changes with a retrieval evaluation harness (Chapter 14).",
  },
  {
    slug: ['part-iv', 'chapter-14-retrieval-evaluation'],
    title: 'Chapter 14 - Retrieval Evaluation',
    description:
      'Measure retrieval quality and grounded answering: recall/precision metrics, citation checks, faithfulness, hallucination detection, and systematic failure analysis.',
    group: 'Part IV - Retrieval Systems',
    toc: [
      'What To Evaluate (And Why)',
      'Build A Test Set (Tutorial)',
      'Retrieval Metrics: Recall, Precision, MRR, nDCG',
      'Answer Quality Metrics: Grounding And Attribution',
      'Citation Quality',
      'Hallucination Detection Approaches',
      'Retrieval Failure Analysis',
      'Online Evaluation And A/B Testing',
      'Regression Testing And Guardrails',
      'Exercises',
      'Summary',
    ],
    body: "# Chapter 14 - Retrieval Evaluation\n\nIf you do not evaluate retrieval, you cannot improve it reliably.\n\nRAG systems fail in ways that \"feel random\" until you measure the pipeline. Evaluation turns random pain into engineering work.\n\nThis chapter teaches you how to:\n\n- build a retrieval test set\n- measure recall and precision at k\n- evaluate grounded answers and citation quality\n- run failure analysis to identify which stage is broken\n\n## What To Evaluate (And Why)\n\nThere are two separate questions:\n\n1. **Did retrieval find the right evidence?**\n2. **Given the evidence, did the model answer correctly and stay grounded?**\n\nDo not mix these. Debug them separately.\n\n\\`\\`\\`text\nRetrieval evaluation: query -> evidence\nGeneration evaluation: evidence -> answer\n\\`\\`\\`\n\nIf an answer is wrong, it could be:\n\n- retrieval miss (the evidence never arrived)\n- packing miss (evidence retrieved but excluded)\n- generation error (evidence present but model misused it)\n\nYour evaluation should help you identify which one.\n\n## Build A Test Set (Tutorial)\n\nYou need a dataset of realistic questions and relevance labels.\n\n### Step 1: Collect Queries\n\nSources:\n\n- search logs (best, if you have them)\n- support tickets\n- internal Q&A channels\n- class exercises (for a student project)\n\nAim for at least 50 queries to start. If this is a production system, you will eventually want hundreds or thousands.\n\n### Step 2: Define What Counts As Relevant\n\nFor each query, define relevance at the *chunk* level or the *document* level.\n\nChunk-level labels are more precise, but harder to create.\n\nA student-friendly approach:\n\n- Label relevant documents first (easier)\n- Then label relevant chunks within the top retrieved documents (more precise)\n\n### Step 3: Create Judgments\n\nFor each query:\n\n- retrieve top 20-50 chunks using your current system\n- have a human label each chunk:\n  - 2 = answers the question directly\n  - 1 = related but not sufficient\n  - 0 = irrelevant\n\nThis is enough to compute most IR metrics.\n\n### Step 4: Store The Dataset\n\nDefine a simple JSON format:\n\n\\`\\`\\`json\n{\n  \"id\": \"q-001\",\n  \"query\": \"How do I rotate API keys?\",\n  \"relevance\": [\n    { \"chunkId\": \"runbook:1200-2400\", \"label\": 2 },\n    { \"chunkId\": \"security-policy:800-1500\", \"label\": 1 }\n  ]\n}\n\\`\\`\\`\n\nImportant: include the chunk ids from your index so evaluation matches reality.\n\n## Retrieval Metrics: Recall, Precision, MRR, nDCG\n\n### Recall@k\n\nRecall@k answers:\n\n> Did the top-k results contain *any* relevant chunk?\n\nFor RAG, recall is often the most important first metric.\n\nExample:\n\n- Query has 1 truly relevant chunk.\n- If it appears in top 10: Recall@10 = 1\n- Otherwise: Recall@10 = 0\n\n### Precision@k\n\nPrecision@k answers:\n\n> How many of the top-k results are relevant?\n\nPrecision matters because irrelevant chunks waste context and can cause hallucinations.\n\n### MRR (Mean Reciprocal Rank)\n\nMRR measures how high the first relevant result appears.\n\n\\`\\`\\`text\nIf first relevant rank is r, reciprocal rank = 1/r\nMRR = average over queries\n\\`\\`\\`\n\nIf you improve MRR, you usually improve context packing because the best evidence arrives early.\n\n### nDCG\n\nnDCG is useful when relevance has levels (2/1/0):\n\n- It rewards placing highly relevant items earlier.\n- It handles graded relevance better than binary recall/precision.\n\n### A Minimal Metric Harness\n\n\\`\\`\\`ts\ntype Judgment = { chunkId: string; label: 0 | 1 | 2 };\ntype EvalQuery = { id: string; query: string; relevance: Judgment[] };\n\nfunction buildRelMap(q: EvalQuery) {\n  const m = new Map<string, number>();\n  for (const j of q.relevance) m.set(j.chunkId, j.label);\n  return m;\n}\n\nexport function recallAtK(q: EvalQuery, retrieved: { chunkId: string }[], k: number) {\n  const rel = buildRelMap(q);\n  for (const r of retrieved.slice(0, k)) {\n    if ((rel.get(r.chunkId) ?? 0) > 0) return 1;\n  }\n  return 0;\n}\n\nexport function precisionAtK(q: EvalQuery, retrieved: { chunkId: string }[], k: number) {\n  const rel = buildRelMap(q);\n  let hits = 0;\n  for (const r of retrieved.slice(0, k)) if ((rel.get(r.chunkId) ?? 0) > 0) hits += 1;\n  return hits / Math.max(1, Math.min(k, retrieved.length));\n}\n\nexport function mrr(q: EvalQuery, retrieved: { chunkId: string }[], k: number) {\n  const rel = buildRelMap(q);\n  const top = retrieved.slice(0, k);\n  for (let i = 0; i < top.length; i++) {\n    if ((rel.get(top[i].chunkId) ?? 0) > 0) return 1 / (i + 1);\n  }\n  return 0;\n}\n\\`\\`\\`\n\nYou can run this harness against different retrieval configs and compare metrics.\n\n## Answer Quality Metrics: Grounding And Attribution\n\nRetrieval metrics do not guarantee good answers.\n\nYou also want to evaluate:\n\n- **Answer relevance**: does it address the question?\n- **Grounding / faithfulness**: is each claim supported by the provided evidence?\n- **Attribution**: are citations provided where required?\n\n### Grounding (Faithfulness) As A Claim Checking Problem\n\nA practical approach:\n\n1. Split the answer into atomic claims.\n2. For each claim, check if it is supported by any provided chunk.\n3. Count unsupported claims as hallucinations.\n\nIn small projects, humans do this. In larger projects, you can use an LLM-as-judge with strict prompts and spot-check.\n\n### A Simple Grounding Rubric\n\nScore 0-2:\n\n- 2: All key claims supported; citations point to correct sources.\n- 1: Mostly supported, but some minor unsupported detail or weak citation.\n- 0: Major claims unsupported or citations misleading.\n\n## Citation Quality\n\nCitation quality is not just \"there is a citation\".\n\nYou care about:\n\n- **Coverage**: do important claims have citations?\n\n- **Correctness**: does the cited chunk actually support the claim?\n\n- **Specificity**: does the citation point to the right doc/section (not a generic overview)?\n\n### Common Citation Failures\n\n1. **Citation laundering**\n\nThe model cites a source that is topically related but does not support the specific claim.\n\n2. **Citation drift**\n\nWhen context packing changes, the model keeps citing old ids/titles.\n\n3. **Over-citation**\n\nThe model cites everything, making citations meaningless.\n\n### A Simple Automated Check\n\nIf you format context as \\`[1] ...\\`, you can require citations like \\`[1]\\` and then verify:\n\n- each cited id exists in the provided context\n- citations appear near the claims\n\nThis does not prove correctness, but it catches obvious issues.\n\n## Hallucination Detection Approaches\n\nThere is no single perfect hallucination detector, but you can combine methods:\n\n1. **Evidence-first prompting**\n\nForce the model to quote or reference evidence before answering.\n\n2. **Answer-with-quotes**\n\nAsk the model to include short quotes from sources. (Then you can string-match quotes.)\n\n3. **Post-hoc claim verification**\n\nRun a second model that checks each claim against the retrieved evidence.\n\n4. **Refusal policy**\n\nWhen evidence is missing or weak, the correct behavior is: \"I cannot find this in the sources.\"\n\nThis is a product decision. Many RAG systems fail because they do not allow refusal.\n\n## Retrieval Failure Analysis\n\nWhen a query fails, classify it. This is the fastest way to improve.\n\n### The Failure Taxonomy\n\n1. **Not indexed**\n\nThe answer exists in source docs but never made it to the index (parser/ingestion bug).\n\n2. **Indexed but unfindable**\n\nThe chunk exists, but retrieval does not return it (embedding mismatch, chunking, filters, query rewrite problems).\n\n3. **Found but not selected**\n\nThe chunk is in candidates, but reranking/selection drops it.\n\n4. **Selected but not used**\n\nThe chunk is in context, but the model ignores or misreads it (prompting or model limitation).\n\n5. **Permission blocked**\n\nThe relevant chunk is filtered out (this might be correct behavior; you need a safe fallback message).\n\n### A Debug Checklist For A Single Bad Query\n\nFor a failed query, capture:\n\n- the query text\n- query embedding model/version\n- retrieval config (k, hybrid weights, filters)\n- top 50 retrieved chunks (ids, scores, titles)\n- final packed context (exact text)\n- model prompt and response\n\nThen answer:\n\n- Was the relevant chunk indexed?\n- If yes, where did it fall in the ranking?\n- If it was retrieved, why did it get dropped?\n- If it was in context, why did the model fail?\n\n### A Practical \"Trace Table\"\n\nCreate a table with one row per query:\n\n\\`\\`\\`text\nquery_id | retrieval_recall@10 | relevant_in_context? | answer_grounded? | notes\n\\`\\`\\`\n\nThis makes it obvious whether improvements should target retrieval or generation.\n\n## Online Evaluation And A/B Testing\n\nOffline evaluation is necessary, but not sufficient.\n\nOnline evaluation tells you how changes affect real users.\n\nCommon online signals:\n\n- user thumbs up/down\n- follow-up question rate (high can mean confusion)\n- time-to-success (did user stop searching?)\n- escalation rate (did they contact support anyway?)\n\n### Guardrails For Online Experiments\n\n- Keep permission checks identical across variants.\n- Keep logging consistent across variants.\n- Roll out gradually and compare cohorts.\n\n## Regression Testing And Guardrails\n\nOnce you have a test set, you can make retrieval changes safely.\n\nRecommended practice:\n\n- pick a primary metric (often Recall@10 for early-stage RAG)\n- require it not to regress beyond a small tolerance\n- track secondary metrics (Precision@10, MRR, groundedness)\n\nAdd \"canary queries\" that must always work (critical policies, security procedures).\n\n## Exercises\n\n1. **Build a mini evaluation set**\n\nCreate 50 queries and label relevance for the top 20 retrieved chunks. Compute Recall@5, Recall@10, Precision@10, and MRR@10.\n\n2. **Chunking regression**\n\nChange chunk size or overlap. Re-run evaluation. Identify which queries improved and which regressed, and classify failures using the taxonomy.\n\n3. **Citation audit**\n\nRequire citations in answers. For 30 queries, manually verify whether citations support the key claims. Record common failure patterns.\n\n4. **Refusal test**\n\nAdd 10 queries that are intentionally unanswerable from the knowledge base. Measure how often the system refuses appropriately vs hallucinating.\n\n## Summary\n\nRetrieval evaluation is how you turn RAG from a demo into an engineering discipline.\n\n- Measure retrieval separately from generation.\n- Start with recall and ranking metrics (Recall@k, MRR, nDCG).\n- Add groundedness and citation quality checks for the full RAG pipeline.\n- Debug failures with a clear taxonomy and logs that preserve intermediate artifacts.",
  },
];
