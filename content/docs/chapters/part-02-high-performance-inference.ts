import type { DocPage } from '../pages';

export const part02HighPerformanceInferencePages: DocPage[] = [
  {
    slug: ['part-2', 'chapter-4-kv-cache'],
    title: 'Chapter 4 - KV Cache',
    description:
      'What the KV cache is, why it dominates GPU memory, and how real servers allocate, reuse, and evict it under load.',
    group: 'Part II - High-Performance Inference',
    toc: [
      'Mental Model',
      'Attention Refresher',
      'What Gets Cached',
      'Shapes and Memory Math',
      'Memory Layout',
      'Prefill vs Decode',
      'Why KV Cache Becomes the Bottleneck',
      'Cache Reuse and Prefix Caching',
      'Eviction, Fragmentation, and Thrashing',
      'Sliding Window and Long-Context',
      'Hard Limits and KV Budgets',
      'Sharing and Isolation',
      'Serving Walkthrough',
      'Failure Modes',
      'Operational Checklist',
      'Exercises',
      'Summary',
    ],
    body: "# Chapter 4 - KV Cache\n\nThis chapter builds a student-friendly, production-accurate mental model of the KV cache:\n\n- **What it stores**\n- **How it grows**\n- **Why it dominates memory**\n- **How real inference servers allocate and evict it**\n- **How it fails in production**\n\nIf you understand KV cache, you can usually explain:\n\n- Why a model OOMs even when weights fit.\n- Why latency spikes during peak traffic.\n- Why \"long outputs\" are expensive even if prompts are short.\n- Why some servers are fast for short prompts but collapse at long context.\n\n---\n\n## Mental Model\n\nThink of an LLM server as doing two phases:\n\n1. **Prefill**: ingest the prompt tokens (compute heavy, parallel).\n2. **Decode**: generate one token at a time (sequential, memory-bandwidth heavy).\n\nThe KV cache is the thing that makes decode feasible: it prevents recomputing attention keys/values for old tokens on every new token.\n\nText diagram:\n\n\\`\\`\\`text\nRequest arrives\n  |\n  v\n[Tokenizer] -> prompt tokens\n  |\n  v\n[Prefill]  (many tokens processed)\n  |\n  +--> write K/V for every layer into KV cache\n  |\n  v\n[Decode loop] (one token at a time)\n  |\n  +--> read old K/V from KV cache\n  +--> compute attention for new token\n  +--> append new token's K/V to KV cache\n  |\n  v\nStream tokens to user\n\\`\\`\\`\n\nImportant: **KV cache grows with (batch size) x (sequence length) x (layers)**. It is not a small constant.\n\n---\n\n## Attention Refresher (Just Enough)\n\nInside a transformer layer, attention computes:\n\n- Queries: Q\n- Keys: K\n- Values: V\n\nFor each token position, attention compares the token's query vector to keys from previous tokens, then mixes values:\n\n\\`\\`\\`text\nscores = softmax(Q * K^T / sqrt(d))\noutput = scores * V\n\\`\\`\\`\n\nIn autoregressive decoding, when you generate token t:\n\n- You need K,V for tokens 1..t (for every layer)\n- But only Q for the new token t\n\nSo caching K and V from past tokens saves a huge amount of work.\n\n---\n\n## What Gets Cached (And What Does Not)\n\nKV cache stores, for each layer:\n\n- K vectors for each token position already processed\n- V vectors for each token position already processed\n\nIt does not store:\n\n- Q (queries are only needed transiently)\n- MLP activations (those are recomputed each decode step)\n- The logits (unless you do something special like logit caching in a narrow workflow)\n\nPractical implication:\n\n- The KV cache is \"per-request state\".\n- Model weights are \"shared state\".\n\nThis is why multi-tenant isolation, admission control, and eviction policies matter.\n\n---\n\n## Shapes and Memory Math (First Principles)\n\nLet:\n\n- L = number of transformer layers\n- H_kv = number of KV heads (can be smaller than attention heads with GQA/MQA)\n- D = head dimension\n- S = sequence length currently cached for a request (prompt tokens + generated tokens so far)\n- B = batch size (number of concurrent sequences sharing a forward pass)\n- bytes = bytes per element (2 for FP16/BF16, 1 for FP8, etc.)\n\nThen KV cache size is approximately:\n\n\\`\\`\\`text\nKV_bytes ~= B * L * S * H_kv * D * 2 * bytes\n                    (K)  (V)\n\\`\\`\\`\n\nThe factor 2 is because we store both K and V.\n\n### Example (Do the math)\n\nSuppose:\n\n- L = 32\n- H_kv = 8 (GQA: 32 attention heads but 8 KV heads)\n- D = 128\n- bytes = 2 (BF16/FP16)\n\nPer token, per sequence:\n\n\\`\\`\\`text\nper_token_bytes = L * H_kv * D * 2 * bytes\n                = 32 * 8 * 128 * 2 * 2\n                = 131,072 bytes\n                = 128 KiB per token per sequence\n\\`\\`\\`\n\nNow at S = 2,000 tokens, one sequence consumes:\n\n\\`\\`\\`text\n128 KiB/token * 2000 tokens = 256,000 KiB ~= 250 MiB\n\\`\\`\\`\n\nThat surprises many people the first time they see it. It means:\n\n- A handful of long-running conversations can eat multiple GB.\n- \"Just let it generate longer\" is a capacity decision, not a UI tweak.\n\n### Why GQA/MQA matters\n\nWith Grouped Query Attention (GQA) or Multi-Query Attention (MQA), you can reduce H_kv:\n\n- **MHA**: H_kv == H_attention\n- **GQA**: H_kv < H_attention (common in many modern LLMs)\n- **MQA**: H_kv == 1\n\nReducing H_kv reduces KV cache memory linearly and often improves decode throughput.\n\n---\n\n## Memory Layout (Why \"Just Allocate a Tensor\" Is Not the Whole Story)\n\nStudents often picture KV cache as a single nice tensor. In a toy implementation, it might be:\n\n- one contiguous K buffer and one contiguous V buffer per request (or per batch)\n- appended each decode step\n\nBut production servers must support:\n\n- sequences joining/leaving continuously\n- variable prompt lengths and output lengths\n- prefill chunking (long prompts)\n- different batch composition each decode step\n\nSo the memory layout becomes a performance feature, not an implementation detail.\n\n### A simple contiguous layout\n\nOne conceptual layout is:\n\n\\`\\`\\`text\nK: [layer][kv_head][token_position][head_dim]\nV: [layer][kv_head][token_position][head_dim]\n\\`\\`\\`\n\nThis is easy to reason about and can be very fast when:\n\n- sequences are long-lived\n- batch membership is stable\n- you do not have much churn\n\nBut with high churn, you tend to get fragmentation or frequent reallocations.\n\n### A paged / block layout\n\nPaged KV breaks the token axis into fixed-size blocks (\"pages\") and stores a per-sequence page table:\n\n\\`\\`\\`text\ntoken positions: 0..S-1\npage size P:     e.g. 16 or 32 tokens per page\n\npages: [0..P-1] [P..2P-1] [2P..3P-1] ...\nseq A page ids: [12, 4, 9, ...]\n\\`\\`\\`\n\nNow attention must gather K/V from non-contiguous pages. This adds addressing overhead, but it makes:\n\n- allocation fast (grab free pages)\n- eviction/preemption possible (free pages)\n- continuous batching viable under heavy churn\n\nIf you see great p50 but terrible p99, or \"free memory but allocation fails\", KV layout and paging behavior are prime suspects.\n\n---\n\n## Prefill vs Decode: Why KV Cache Changes the Performance Profile\n\nPrefill and decode stress different hardware resources.\n\n### Prefill (prompt ingestion)\n\n- Processes many tokens in parallel.\n- More compute-heavy (matrix multiplies).\n- Strongly benefits from larger batch sizes.\n- Often dominated by tensor cores / compute throughput.\n\n### Decode (generation)\n\n- Processes 1 new token per sequence per step.\n- Must attend over all cached tokens (S grows).\n- Becomes memory-bandwidth and latency sensitive.\n- The KV cache is read constantly, and appended each step.\n\nText diagram of growth:\n\n\\`\\`\\`text\ntime ->    prefill           decode decode decode ...\nS ->      prompt_len         +1     +1     +1\nKV ->     allocate+write     read+append each step\n\\`\\`\\`\n\nResult: servers that look \"fast\" on prefill benchmarks can still be slow in interactive chat if decode is not optimized.\n\n---\n\n## Why KV Cache Becomes the Bottleneck\n\nThere are three practical reasons KV cache is frequently the first thing to break:\n\n1. **It scales with live traffic**, not with model size.\n2. **It scales with output length**, which is often unbounded without strict limits.\n3. **It is allocated in large, contiguous-ish GPU memory regions**, making fragmentation and allocator behavior relevant.\n\n### Weight memory vs KV memory\n\nWeights are roughly fixed:\n\n\\`\\`\\`text\nweights_bytes ~= params * bytes_per_param\n\\`\\`\\`\n\nKV cache is variable:\n\n\\`\\`\\`text\nKV_bytes ~= live_sequences * avg_sequence_len * per_token_bytes\n\\`\\`\\`\n\nThis means your capacity planning must include **traffic shape**:\n\n- Many short requests: throughput-focused, KV smaller.\n- Fewer long chat sessions: KV dominates, latency spikes.\n- Mixed workload: scheduling and fragmentation become major issues.\n\n---\n\n## Cache Reuse and Prefix Caching (The Good Kind of Sharing)\n\nIf many requests share an identical prefix, you can reuse KV cache for that prefix.\n\nCommon sources of shared prefixes:\n\n- A large system prompt\n- A fixed set of instruction tokens\n- A long \"policy\" prefix\n- A document chunk reused across many questions\n\n### Idea: compute once, reuse many\n\n\\`\\`\\`text\nprefix tokens -> prefill once -> store prefix KV\nrequest i -> attach prefix KV + prefill only the unique suffix\n\\`\\`\\`\n\n### When it works well\n\n- Prefix is truly identical at the token level (tokenization must match).\n- Prefix is large enough that reuse saves time and money.\n- Workload has repeated structure (e.g., a support bot with a stable system prompt).\n\n### When it does not\n\n- Slight differences in whitespace or templated variables change tokenization.\n- You are doing per-user personalization in the prefix.\n- You include timestamps, unique IDs, or request-specific \"hidden notes\" in the prefix.\n\n### Practical tip: make the shared prefix stable\n\nIf you want prefix caching, treat your system prompt as a versioned artifact:\n\n- Keep it stable across requests.\n- When you change it, bump a version string and expect cache misses.\n\n---\n\n## Eviction, Fragmentation, and Thrashing (The Bad Kind of Reality)\n\nKV cache lives in GPU memory. GPU memory is finite. So servers need policies for:\n\n- **Admission control**: should we accept a new request?\n- **Eviction**: what gets removed when memory is tight?\n- **Compaction / paging**: how do we avoid fragmentation and OOM?\n\n### Fragmentation (what it feels like)\n\nYou have \"free memory\", but you cannot allocate a large block.\n\n\\`\\`\\`text\nGPU memory:\n[used][free][used][free][used][free]\n\nneed a big contiguous allocation for a new sequence:\n                X (fails)\n\\`\\`\\`\n\nSome modern servers avoid this by using paging (block-based allocation) rather than one big contiguous KV buffer per sequence.\n\n### Thrashing (what it looks like in metrics)\n\nIf your server starts swapping KV blocks, preempting sequences, or repeatedly evicting/rehydrating state, you see:\n\n- tail latency explosion (p95/p99 jump)\n- throughput collapse\n- GPU utilization oscillation\n- lots of \"cache miss\" or \"block swap\" counters (server dependent)\n\n### A simple eviction policy (conceptual)\n\nWhen memory pressure increases:\n\n1. Evict finished requests immediately.\n2. Reject new requests once you hit a KV utilization threshold.\n3. Preempt or slow low-priority requests.\n4. Enforce strict max output tokens.\n\nIn real systems, eviction interacts with fairness and user experience.\n\n---\n\n## Sliding Window and Long-Context Strategies\n\nYou cannot keep infinitely growing KV cache forever. Common strategies:\n\n### 1) Hard limit\n\nStop generation when you hit max tokens or context window.\n\nPros: simple, predictable.\nCons: user-visible truncation.\n\n### 2) Sliding window attention\n\nKeep only the most recent W tokens in the KV cache.\n\n\\`\\`\\`text\ncontext: [old ... old][recent recent recent]\nKV kept:               [recent recent recent]\n\\`\\`\\`\n\nPros: bounded memory.\nCons: model cannot attend to very old tokens (quality drop on long dependencies).\n\n### 3) Summarize / compress old context (outside the model)\n\n- Summarize earlier conversation into fewer tokens.\n- Keep \"facts\" in a structured memory.\n- Retrieve relevant pieces later (RAG).\n\nPros: can preserve meaning with fewer tokens.\nCons: additional pipeline complexity, potential summary errors.\n\nThe key engineering point: these are not purely \"prompt engineering\" choices. They are **memory management** choices that determine server capacity.\n\n---\n\n## Practical Serving Example: Hard Limits and KV Budgets\n\nThis section is intentionally generic (APIs and flags differ across stacks), but it shows the exact *shape* of production controls that prevent KV cache failures.\n\n### 1) Enforce budgets at the API edge (reject early)\n\nAt admission time, enforce strict limits:\n\n- \\`max_input_tokens\\`\n- \\`max_output_tokens\\`\n- \\`max_concurrent_requests_per_tenant\\`\n- \\`max_requests_per_minute_per_tenant\\`\n\nEarly rejection is cheaper than OOM mid-generation.\n\n### 2) Admission control by estimated KV footprint\n\nYou already computed:\n\n\\`\\`\\`text\nper_token_bytes = L * H_kv * D * 2 * bytes\n\\`\\`\\`\n\nEstimate per request:\n\n\\`\\`\\`text\nestimated_total_tokens = prompt_tokens + max_output_tokens\nestimated_kv_bytes = estimated_total_tokens * per_token_bytes\n\\`\\`\\`\n\nThen accept only if you have headroom:\n\n\\`\\`\\`text\naccept if (current_kv_bytes + estimated_kv_bytes) < kv_budget_bytes\n\\`\\`\\`\n\n### 3) A \"server style\" config sketch (what you should expect to have)\n\n\\`\\`\\`text\nMAX_INPUT_TOKENS=4096\nMAX_OUTPUT_TOKENS=512\nMAX_CONCURRENCY=32\nSCHEDULER=continuous_batching\nPREFILL_CHUNK_TOKENS=512\nKV_BUDGET_GB=10\n\\`\\`\\`\n\nEven if your system does not use these exact names, you want these knobs somewhere in your stack.\n\n### 4) What to log for debugging\n\nLog per request:\n\n- prompt_tokens\n- max_output_tokens\n- actual_output_tokens\n- TTFT (time to first token)\n- decode_tokens_per_second\n- whether any paging/preemption happened (if your server exposes it)\n\nThese logs make \"why did p99 spike\" answerable.\n\n---\n\n## Sharing and Isolation: Multi-Tenant Reality\n\nIn a multi-tenant system, KV cache is one of your biggest isolation risks.\n\nYou must answer questions like:\n\n- Can one tenant starve others by starting many long generations?\n- Do you enforce per-tenant KV budgets?\n- Do you allow \"best effort\" long generations or always enforce strict limits?\n\n### Recommended mental model: KV cache is a billable resource\n\nEven if you do not literally bill users, treat it like:\n\n- CPU time\n- GPU time\n- DB connections\n\nBecause it is: it consumes a scarce shared pool.\n\n---\n\n## Serving Walkthrough: Estimating Capacity Before You Deploy\n\nYou can do a back-of-the-envelope capacity estimate using just a few numbers.\n\n### Step 1: compute per-token KV bytes (per sequence)\n\n\\`\\`\\`text\nper_token_bytes = L * H_kv * D * 2 * bytes\n\\`\\`\\`\n\n### Step 2: decide a target \"average live sequence length\"\n\nFor chat, use:\n\n\\`\\`\\`text\navg_live_tokens = avg_prompt_tokens + avg_generated_so_far\n\\`\\`\\`\n\nBe careful: during peak usage, many requests are mid-generation, so \"generated so far\" is not near zero.\n\n### Step 3: decide max concurrent sequences\n\nThen:\n\n\\`\\`\\`text\nKV_total ~= concurrent_sequences * avg_live_tokens * per_token_bytes\n\\`\\`\\`\n\nIf KV_total approaches your GPU memory headroom (after weights, buffers, and fragmentation), you will see OOM or preemption.\n\n### Concrete example\n\nAssume:\n\n- per_token_bytes = 128 KiB (from earlier example)\n- avg_live_tokens = 1,500\n- concurrent_sequences = 20\n\n\\`\\`\\`text\nKV_total ~= 20 * 1500 * 128 KiB\n         ~= 3,840,000 KiB\n         ~= ~3.7 GiB\n\\`\\`\\`\n\nThis is just KV. If your weights are 14 GiB and you have overhead, a 24 GiB GPU is now tight.\n\n---\n\n## Production Failure Modes (What Breaks First)\n\n### 1) OOM on long outputs\n\nSymptoms:\n\n- OOM errors occur only on some requests.\n- They correlate with long generations, not large prompts.\n\nFixes:\n\n- Set strict max output tokens.\n- Add admission control by estimated KV footprint.\n- Add per-tenant concurrency limits.\n\n### 2) Tail latency spikes at moderate load\n\nSymptoms:\n\n- p50 looks fine; p99 becomes unusable.\n- More frequent during mixed short/long traffic.\n\nLikely causes:\n\n- scheduling unfairness (long requests hog decode steps)\n- cache paging / swaps / preemptions\n- fragmentation causing allocator slow paths\n\nFixes:\n\n- continuous batching with fairness scheduling\n- cap max generation length for interactive tiers\n- separate pools (interactive vs batch) to reduce interference\n\n### 3) \"Free memory\" but allocation fails\n\nSymptoms:\n\n- monitoring shows available memory.\n- allocations fail for new sequences.\n\nCause:\n\n- fragmentation (especially with per-sequence contiguous allocations)\n\nFixes:\n\n- prefer paging/block allocators\n- restart server as a last resort (not a plan, but sometimes reality)\n- reduce workload variance (cap lengths, normalize prompt sizes)\n\n### 4) Users see \"context forgetting\"\n\nSymptoms:\n\n- model ignores early conversation\n\nCause:\n\n- sliding window / truncated context / server-side eviction policy\n\nFix:\n\n- be explicit in product and UX about limits\n- add summarization/RAG memory strategy rather than silently dropping tokens\n\n---\n\n## Operational Checklist\n\nBefore shipping an LLM server with multiple concurrent users:\n\n1. Set hard limits: max input tokens and max output tokens.\n2. Measure typical prompt and output lengths in your real product.\n3. Compute KV per-token bytes for your model (account for GQA/MQA).\n4. Leave GPU memory headroom for fragmentation and temporary buffers.\n5. Enforce per-tenant and global concurrency limits.\n6. Track p50/p95/p99 latency separately for prefill and decode.\n7. Add alarms for:\n   - KV utilization (or proxy: GPU mem used)\n   - OOM frequency\n   - tail latency\n   - queue depth / admission rejections\n8. Plan for mixed workloads: isolate interactive traffic from batch jobs.\n\n---\n\n## Exercises\n\n1. **KV cache sizing**: Pick a popular open model configuration (L, H_kv, D). Compute per-token KV bytes for FP16 and for FP8. How many concurrent 2,000-token sequences fit in 24 GiB if weights take 14 GiB?\n2. **Workload shift**: You launch a new feature that increases average output length from 200 tokens to 800 tokens. What happens to:\n   - KV memory consumption\n   - throughput\n   - tail latency\n3. **Prefix caching experiment design**: In your app, list which parts of the prompt are stable vs dynamic. How would you restructure prompts to maximize shared prefixes without harming product correctness?\n4. **Failure mode mapping**: For each failure mode in this chapter, write the metric you would alert on and the mitigation you would deploy first.\n\n---\n\n## Summary\n\n- The **KV cache stores keys and values** for every processed token at every layer.\n- KV cache memory grows with **(concurrency) x (sequence length) x (layers)** and often dominates GPU memory.\n- Prefill is compute-heavy; decode is often **memory-bandwidth and KV-read heavy**.\n- Real servers must manage KV cache with **paging, admission control, eviction, and fairness**, or tail latency and OOM will appear.\n- Treat KV cache as a first-class production resource: budget it, monitor it, and design product limits around it.",
  },
  {
    slug: ['part-2', 'chapter-5-efficient-inference'],
    title: 'Chapter 5 - Efficient Inference',
    description:
      'Dynamic and continuous batching, paged attention, FlashAttention, and GPU scheduling strategies that turn theory into real throughput.',
    group: 'Part II - High-Performance Inference',
    toc: [
      'The Goal: Throughput Without Killing Latency',
      'Where Time Goes in an LLM Server',
      'Batching Basics',
      'Dynamic Batching',
      'Continuous Batching',
      'Prefill Chunking',
      'Paged Attention and Block KV',
      'FlashAttention and Why It Matters',
      'Scheduling and Fairness',
      'GPU Parallelism (TP/PP/DP)',
      'Practical Serving Examples',
      'Hands-On Serving Sketch',
      'Production Failure Modes',
      'Optimization Checklist',
      'Exercises',
      'Summary',
    ],
    body: "# Chapter 5 - Efficient Inference\n\nThis chapter is about getting real performance out of an LLM server.\n\nYou can think of it as a set of levers:\n\n- **Batching** to use the GPU efficiently.\n- **Memory-aware attention kernels** to keep KV reads fast.\n- **Scheduling** to avoid tail-latency disasters.\n- **Parallelism** to run models too large for a single GPU.\n\nWe will keep the focus on student-friendly first principles, then connect them to production behaviors and concrete serving choices.\n\n---\n\n## The Goal: Throughput Without Killing Latency\n\nIn production you usually care about two things at the same time:\n\n- **Latency**: how long a user waits for the first token and the full answer.\n- **Throughput**: how many tokens per second your fleet can generate.\n\nThese trade off because batching increases throughput but can add queueing delay.\n\nText diagram (latency decomposition):\n\n\\`\\`\\`text\nend-to-end latency\n  = queue_wait\n  + prefill_time (prompt ingestion)\n  + decode_time (generation)\n  + network + stream overhead\n\\`\\`\\`\n\nYour job is to:\n\n- keep queue_wait small for interactive traffic\n- keep the GPU busy enough that you get good throughput\n\n---\n\n## Where Time Goes in an LLM Server\n\nAn LLM server repeats a loop:\n\n1. Collect requests.\n2. Tokenize.\n3. Run prefill for prompt tokens.\n4. Run decode steps until each request finishes.\n\nThe key is that **prefill and decode have different optimal batching**:\n\n- Prefill: many tokens, good parallelism.\n- Decode: one token per sequence per step, limited parallelism unless you batch multiple sequences.\n\nSo \"efficient inference\" usually means \"make decode efficient under concurrency\".\n\n---\n\n## Batching Basics (Why Batches Help)\n\nGPUs like large matrix operations. If you only run one sequence at a time during decode:\n\n- You do a small amount of compute each step.\n- You do a lot of memory traffic (reading KV).\n- Your GPU sits underutilized.\n\nBatching multiple sequences together creates larger operations and amortizes overhead.\n\nBut naive batching also creates pain:\n\n- requests have different prompt lengths\n- requests finish at different times\n- queueing delay grows\n\nSo we need smarter batching.\n\n---\n\n## Dynamic Batching (Microbatching)\n\nDynamic batching groups requests that arrive within a small time window into a batch.\n\nYou control it with:\n\n- a **max batch size**\n- a **max waiting time** (batching window)\n\nText diagram:\n\n\\`\\`\\`text\nrequests arrive:  A  B   C     D\nbatch window:    [----]\nbatch formed:    [A B C]\n\\`\\`\\`\n\n### Pros\n\n- Simple.\n- Works well for short requests and steady traffic.\n\n### Cons\n\n- Hard with variable-length sequences.\n- During decode, sequences diverge (some finish early, some keep going).\n- Can cause head-of-line blocking if you wait too long to fill a batch.\n\nDynamic batching is often a baseline, not the end state.\n\n---\n\n## Continuous Batching (The Modern Pattern)\n\nContinuous batching (sometimes called \"iteration-level scheduling\") treats decode as a repeating step:\n\n- each step generates 1 token for many sequences\n- after each step, finished sequences leave\n- new sequences can enter\n\nText diagram:\n\n\\`\\`\\`text\nstep 1: [A B C]\nstep 2: [A B C D]    (D joins)\nstep 3: [A C D]      (B finished)\nstep 4: [A C D E]    (E joins)\n\\`\\`\\`\n\nThis is a huge deal because it:\n\n- keeps the GPU busy\n- avoids waiting for batches to \"drain\"\n- can significantly improve throughput and reduce queueing under load\n\n### What makes it hard\n\nYou need:\n\n- a scheduler that decides which sequences get a decode slot each step\n- a KV memory manager that can handle sequences entering/leaving frequently\n- often, a paged/block KV cache allocator to avoid fragmentation\n\n---\n\n## Prefill Chunking (A Latency/Throughput Trick)\n\nLong prompts create a prefill problem:\n\n- if you prefill a very long prompt in one go, you may monopolize the GPU\n- other requests wait, hurting tail latency\n\nPrefill chunking splits a long prefill into smaller pieces:\n\n\\`\\`\\`text\nprefill tokens: [................. long prompt .................]\nchunk into:     [....][....][....][....]\n\\`\\`\\`\n\nScheduler interleaves these chunks with decode steps from other requests.\n\n### Why students should care\n\nPrefill chunking is an example of a general idea:\n\n- \"optimize for p99\" by preventing any single request from monopolizing the GPU.\n\n---\n\n## Paged Attention and Block KV (Solving Fragmentation)\n\nIf each sequence had a big contiguous KV allocation, continuous batching would fragment memory quickly.\n\nPaged/block KV allocators store KV cache in fixed-size blocks (pages) and map each sequence to a list of blocks:\n\n\\`\\`\\`text\nKV blocks (fixed size):\n[B0][B1][B2][B3][B4][B5]...\n\nSequence A uses blocks: B1 -> B4 -> B2 ...\nSequence B uses blocks: B0 -> B3 ...\n\\`\\`\\`\n\nBenefits:\n\n- allocation is fast (grab a few blocks)\n- fragmentation is reduced\n- eviction/preemption is easier (free blocks)\n\nCost:\n\n- more complex addressing logic in attention kernels\n- potential overhead if blocks are too small or too large\n\nPaged attention is a kernel-level and allocator-level co-design: you get the biggest wins when your attention kernel is built to gather K/V from blocks efficiently.\n\n---\n\n## FlashAttention and Why It Matters\n\nAttention can be expensive because it forms an S x S matrix conceptually (sequence length squared).\n\nFlashAttention-style kernels avoid materializing large intermediate matrices in GPU memory:\n\n- they compute attention in tiles\n- they use GPU SRAM/shared memory effectively\n- they reduce memory bandwidth and improve speed\n\nStudent intuition:\n\n- many performance wins come from \"do not write large temporary tensors to HBM\"\n- instead, fuse operations and compute on the fly\n\nIn practice, better attention kernels improve:\n\n- prefill speed (especially at larger S)\n- sometimes decode speed (depending on kernel and batch characteristics)\n\n---\n\n## Scheduling and Fairness (Avoiding Tail Latency Disasters)\n\nSchedulers decide which sequences get compute each decode iteration.\n\nIf you maximize raw throughput with no fairness:\n\n- long requests can dominate\n- short interactive requests suffer\n\nA common production goal is:\n\n- keep interactive p99 low\n- still keep GPU utilization high\n\n### Typical policies (conceptual)\n\n1. **First-come first-served**: simple, can be unfair.\n2. **Shortest remaining**: good latency, but hard to know remaining length.\n3. **Token-based quotas**: each request gets a budget per time slice.\n4. **Priority tiers**: interactive vs batch pools.\n\n### Practical split: two queues\n\nIf you have both:\n\n- chat UI traffic (interactive)\n- offline jobs (summarization, indexing, eval)\n\nRun them in separate pools (or separate priority queues) so batch does not blow up interactive tail latency.\n\n---\n\n## GPU Parallelism (TP/PP/DP) in One Page\n\nSome models do not fit on one GPU. You then use parallelism.\n\n### Tensor Parallel (TP)\n\nSplit layers' matrix multiplications across GPUs.\n\n- Pros: works well for single request latency.\n- Cons: requires fast interconnect, adds communication overhead.\n\n### Pipeline Parallel (PP)\n\nSplit layers into stages across GPUs.\n\n- Pros: can reduce per-GPU memory.\n- Cons: increases latency; complex scheduling; bubbles in the pipeline.\n\n### Data Parallel (DP)\n\nReplicate model on multiple GPUs and split traffic.\n\n- Pros: simple scaling for throughput.\n- Cons: each GPU must fit the full model weights.\n\nText diagram:\n\n\\`\\`\\`text\nDP:   [GPU1: full model] [GPU2: full model]\nTP:   [GPU1: shard]      [GPU2: shard]\nPP:   [GPU1: early layers] -> [GPU2: later layers]\n\\`\\`\\`\n\nIn real deployments you often mix them (e.g., TP inside a node, DP across nodes).\n\n---\n\n## Practical Serving Examples (Patterns, Not Brand Worship)\n\nThe exact flags differ across servers, but the design patterns are stable. Here are practical examples in a \"serving cookbook\" style.\n\n### Example 0: Benchmark the right thing (a minimal playbook)\n\nBefore tuning, benchmark a workload that matches reality:\n\n\\`\\`\\`text\nInputs to define:\n  - prompt length distribution (p50/p95)\n  - output length distribution (p50/p95)\n  - concurrency target (interactive vs batch)\n\nMetrics to capture:\n  - TTFT p50/p95/p99\n  - end-to-end latency p50/p95/p99\n  - steady-state decode tokens/sec\n  - GPU memory used and peaks\n  - GPU utilization (and memory bandwidth if you can observe it)\n\\`\\`\\`\n\nIf you only benchmark \"single request tokens/sec\", you will miss queueing, KV pressure, and fairness problems that dominate production.\n\n### Example A: Separate interactive and batch pools\n\nRun two deployments of the same model:\n\n- **interactive**: lower max output tokens, stricter timeouts, smaller queue.\n- **batch**: higher max output tokens, larger queue, better throughput focus.\n\nThis avoids one of the most common production failures: batch workloads destroying chat UX.\n\n### Example B: Enforce budgets at the API layer\n\nRegardless of server, enforce:\n\n- max input tokens\n- max output tokens\n- max concurrent requests per tenant\n- max requests per minute per tenant\n\nThis is not \"nice to have\". It is protection for your KV cache, GPU time, and customer experience.\n\n### Example C: Token-based admission control\n\nA simple admission heuristic:\n\n\\`\\`\\`text\nestimated_kv_tokens = prompt_tokens + max_output_tokens\nestimated_kv_bytes = estimated_kv_tokens * per_token_bytes\naccept if (current_kv_bytes + estimated_kv_bytes) < kv_budget\n\\`\\`\\`\n\nThis prevents admitting a request that is likely to OOM the GPU mid-generation.\n\n### Example D: Observe prefill vs decode separately\n\nInstrument:\n\n- time-to-first-token (TTFT): dominated by queue + prefill\n- tokens/sec during decode: dominated by batching + KV efficiency\n\nIf TTFT is bad, you likely have:\n\n- queueing\n- slow prefill (long prompts or weak kernels)\n\nIf decode rate is bad, you likely have:\n\n- insufficient batching\n- KV cache pressure / paging\n- inefficient attention kernels\n\n---\n\n## Hands-On Serving Sketch: A Minimal Chat Completion API\n\nThis is a conceptual end-to-end flow that matches most production systems, whether you use a hosted API or self-host.\n\n### Request structure\n\n\\`\\`\\`json\n{\n  \"model\": \"your-model\",\n  \"messages\": [\n    { \"role\": \"system\", \"content\": \"You are a helpful assistant.\" },\n    { \"role\": \"user\", \"content\": \"Explain KV cache in two paragraphs.\" }\n  ],\n  \"max_output_tokens\": 256,\n  \"temperature\": 0.2,\n  \"stream\": true\n}\n\\`\\`\\`\n\n### Server-side steps\n\n\\`\\`\\`text\n1) tokenize(messages) -> prompt_tokens\n2) enforce token limits + tenant budgets\n3) enqueue request into interactive queue\n4) scheduler chooses it for prefill (possibly chunked)\n5) scheduler runs decode steps (continuous batching)\n6) stream tokens back as they are produced\n\\`\\`\\`\n\n### What to tune first (pragmatic ordering)\n\n1. Token limits + admission control (prevents disasters).\n2. Continuous batching + fairness scheduling (improves p99 under load).\n3. Paged/block KV + attention kernels (improves throughput with churn).\n4. Parallelism strategy (only if needed to fit or scale beyond one GPU).\n\n---\n\n## Production Failure Modes\n\n### 1) Head-of-line blocking\n\nCause:\n\n- batching or scheduling lets one huge prompt monopolize prefill\n\nFix:\n\n- prefill chunking\n- separate traffic classes\n\n### 2) Tail latency explosion from mixed lengths\n\nCause:\n\n- long generations keep sequences alive (KV grows), consuming memory and decode slots\n\nFix:\n\n- cap output length for interactive tier\n- admission control by KV footprint\n- priority scheduling\n\n### 3) GPU looks busy but throughput is low\n\nCause:\n\n- memory bandwidth bound (KV reads)\n- poor kernel efficiency\n- too-small effective batch size in decode\n\nFix:\n\n- continuous batching\n- better attention kernels (FlashAttention-class)\n- reduce H_kv via model choice (GQA/MQA)\n- quantization can sometimes help by lowering memory traffic (but can also slow if kernels are weak)\n\n### 4) Unstable performance (oscillations)\n\nCause:\n\n- scheduler feedback loops\n- paging/preemption thrash under near-OOM\n\nFix:\n\n- reduce target KV utilization (leave headroom)\n- stricter admission control\n- reduce workload variance\n\n---\n\n## Optimization Checklist\n\nUse this checklist when you are tuning a model server:\n\n1. Set strict token limits and enforce them at the API edge.\n2. Measure TTFT and decode tokens/sec separately.\n3. Use continuous batching for interactive workloads.\n4. Add prefill chunking if long prompts exist.\n5. Prefer paged/block KV allocation to reduce fragmentation.\n6. Ensure you use efficient attention kernels (FlashAttention-class) where possible.\n7. Choose a scheduling policy that preserves fairness (or separate pools).\n8. Plan parallelism deliberately: DP for throughput, TP/PP for fitting large models.\n9. Leave GPU memory headroom for fragmentation, temporary buffers, and spikes.\n10. Verify improvements with real traffic shape, not only synthetic benchmarks.\n\n---\n\n## Exercises\n\n1. **Latency decomposition**: In your own words, explain which parts of latency affect TTFT vs the steady-state decode rate. Give one mitigation for each.\n2. **Batching tradeoff**: Suppose you increase the batching window from 5ms to 25ms. What improves? What gets worse? When is it acceptable?\n3. **Continuous batching thought experiment**: Create a step-by-step schedule for 5 requests of different lengths over 6 decode iterations. Show which requests are present each step and which finish.\n4. **Capacity plan**: Using the KV math from Chapter 4, estimate how many concurrent sequences you can support on a single GPU given your token limits.\n\n---\n\n## Summary\n\n- Efficient inference is mostly about **making decode efficient under concurrency**.\n- **Continuous batching** is a core modern technique: requests can join/leave each decode step.\n- **Prefill chunking** prevents long prompts from monopolizing the GPU.\n- **Paged/block KV** reduces fragmentation and enables high-churn scheduling.\n- **FlashAttention-class kernels** win by reducing memory traffic and avoiding large intermediates.\n- Production success requires combining kernels, scheduling, and strict product-level token limits.",
  },
  {
    slug: ['part-2', 'chapter-6-faster-generation'],
    title: 'Chapter 6 - Faster Generation',
    description:
      'Speculative decoding and other techniques that reduce expensive verifier tokens, plus streaming and system-level tricks for perceived speed.',
    group: 'Part II - High-Performance Inference',
    toc: [
      'What "Faster" Means',
      'Decode is the Tax',
      'Speculative Decoding Intuition',
      'Speculative Decoding Pseudocode',
      'The Algorithm (Step by Step)',
      'Draft/Verify Tradeoffs',
      'Choosing a Draft Model',
      'Assisted Generation Patterns',
      'Parallel Decoding and Variants',
      'Streaming Optimizations',
      'Failure Modes',
      'Deployment Checklist',
      'Exercises',
      'Summary',
    ],
    body: "# Chapter 6 - Faster Generation\n\nWhen people ask for \"faster LLMs\", they usually mean at least one of these:\n\n1. **Lower time-to-first-token (TTFT)**: the user sees output sooner.\n2. **Higher tokens/sec**: the answer finishes sooner.\n3. **Lower cost per token**: you can scale without a bigger budget.\n\nThis chapter focuses on speeding up the decode phase, because decode is where:\n\n- KV cache reads dominate,\n- batching limits show up,\n- and latency is most visible to users.\n\n---\n\n## Decode is the Tax\n\nEven with perfect batching and kernels, decode has a structural limitation:\n\n- autoregressive generation is sequential\n- each new token depends on previous tokens\n\nSo the high-level question is:\n\n> How can we produce multiple tokens' worth of progress per expensive model step?\n\nOne powerful answer is **speculative decoding**.\n\n---\n\n## Speculative Decoding Intuition\n\nImagine you have:\n\n- a **big model** (slow, accurate): the verifier\n- a **small model** (fast, less accurate): the draft\n\nInstead of having the big model generate every token, we let the draft propose several tokens at once, then we ask the big model to verify them efficiently.\n\nText diagram:\n\n\\`\\`\\`text\nDraft model (fast):     proposes  k tokens:  t1 t2 t3 ... tk\nVerifier model (slow):  checks them and accepts a prefix\n\\`\\`\\`\n\nIf the draft is often correct, the verifier can accept many tokens per verifier step, increasing effective throughput.\n\nImportant: speculative decoding does not magically remove computation. It rearranges it:\n\n- you spend extra compute on the draft model\n- but you reduce how often you must run expensive verifier decode steps\n\n---\n\n## Speculative Decoding Pseudocode (Readable, Not Library-Specific)\n\nThis pseudocode is meant to teach the control flow. It ignores many engineering details (GPU batching, KV management, and exact sampling), but it matches what you should picture.\n\n\\`\\`\\`text\ngiven:\n  verifier_model V\n  draft_model    D\n  context tokens C\n  proposal length k\n\nloop until finished:\n  # 1) Draft proposes k tokens autoregressively\n  proposal = []\n  for i in 1..k:\n    proposal.append( sample(D, C + proposal) )\n\n  # 2) Verifier checks the proposal positions\n  accept_count = 0\n  for i in 1..k:\n    verifier_next = sample(V, C + proposal[0..i-1])\n    if verifier_next == proposal[i]:\n      accept_count += 1\n    else:\n      break\n\n  # 3) Commit accepted tokens\n  C = C + proposal[0..accept_count-1]\n\n  # 4) If rejected early, emit verifier token at first mismatch position\n  if accept_count < k:\n    C = C + verifier_next\n\\`\\`\\`\n\nWhat this teaches:\n\n- draft pays for proposing tokens\n- verifier pays for checking them\n- speedup depends on how many tokens you accept per verifier work\n\n---\n\n## The Algorithm (Step by Step)\n\nWe will describe a common form of speculative decoding conceptually (implementations vary, but this mental model matches the main idea).\n\nAssume we have current context C.\n\n### Step 1: Draft proposes k tokens\n\nDraft model runs autoregressively for k steps:\n\n\\`\\`\\`text\ndraft(C) -> d1\ndraft(C d1) -> d2\n...\ndraft(C d1..d{k-1}) -> dk\n\\`\\`\\`\n\nNow we have a proposed continuation D = d1..dk.\n\n### Step 2: Verifier checks the proposal\n\nVerifier computes the probability distribution for the next token at each position in the proposal, often in a batched way:\n\n\\`\\`\\`text\nverifier predicts next-token logits for:\n  position after C,\n  position after C d1,\n  position after C d1 d2,\n  ...\n\\`\\`\\`\n\n### Step 3: Accept a prefix, then continue\n\nWe compare the draft tokens to what the verifier would have generated.\n\n- If the verifier \"agrees\" with d1, we accept it.\n- Continue for d2, d3...\n- When we find a token where the draft is not acceptable, we stop accepting, and we sample the verifier token for that position.\n\nSo each cycle produces:\n\n- several accepted draft tokens\n- plus possibly 1 verifier token at the end of the cycle\n\n### Why it can speed things up\n\nIn the best case, you accept most of the k draft tokens frequently. That means one verifier pass produces multiple tokens of progress.\n\nIn the worst case (draft is poor), you accept few tokens and wasted work on the draft model.\n\n---\n\n## Draft/Verify Tradeoffs (What Controls Speedup)\n\nSpeculative decoding speedup depends on:\n\n1. **Draft speed**: draft model must be significantly faster than verifier.\n2. **Acceptance rate**: how often draft tokens are accepted.\n3. **k (proposal length)**: larger k can increase gains but also increases risk of rejection and overhead.\n\nA simplified intuition:\n\n\\`\\`\\`text\neffective_tokens_per_verifier_step ~= accepted_tokens + 1\n\\`\\`\\`\n\nIf you accept on average 3 draft tokens per cycle, you might get ~4 tokens of progress per verifier step. That can be a big win.\n\nBut acceptance rate is sensitive to:\n\n- how similar the draft model is to the verifier model (training, architecture, tokenizer)\n- decoding settings (temperature/top-p)\n- domain shift (chat vs code vs math)\n\n---\n\n## Choosing a Draft Model (Student Decision Framework)\n\nYou want the draft model to be:\n\n- cheap and fast\n- but aligned enough that acceptance rate is high\n\nGood candidates:\n\n- a smaller model in the same family\n- a distilled version\n- a quantized draft model (if kernels are good)\n\nPotential pitfalls:\n\n- different tokenizers cause mismatch and lower acceptance\n- draft model too small => low acceptance => no speedup\n- draft model too large => draft cost cancels verifier savings\n\nPractical rule of thumb:\n\n- draft model should be much smaller than verifier\n- but not so small that it makes nonsense in your domain\n\n---\n\n## Assisted Generation Patterns (Beyond Classic Speculation)\n\nThere are other \"assist the big model\" techniques that share the same spirit:\n\n### Pattern 1: Heuristic drafts\n\nFor structured tasks, you can draft common tokens without a model:\n\n- JSON braces and punctuation\n- fixed schema keys\n- boilerplate templates\n\nThen verify with the model or constrain generation so the model follows the structure.\n\n### Pattern 2: Retrieval-assisted drafting\n\nIf you know the answer likely quotes from a document:\n\n- retrieve text spans\n- propose them as drafts (or as high-probability candidates)\n- let the model verify or adapt\n\nThis is not always \"speculative decoding\" in the strict sense, but it can reduce the amount of expensive novel generation.\n\n### Pattern 3: Constrained decoding\n\nIf the output must follow a grammar/schema, constrain decoding so the model does not waste tokens exploring invalid branches.\n\nThis improves:\n\n- quality (fewer malformed outputs)\n- effective speed (fewer retries)\n\n---\n\n## Parallel Decoding and Variants (Conceptual Overview)\n\nResearchers and systems builders have explored ways to produce multiple tokens per step:\n\n- parallel proposals\n- tree-based candidate expansion\n- lookahead decoding with verification\n\nMany of these are complicated, and their production value depends on:\n\n- kernel support\n- implementation complexity\n- whether they preserve exact distribution or approximate it\n\nFor most students building real systems today:\n\n- speculative decoding is the most practical high-impact technique to learn deeply\n- plus good batching/attention kernels from Chapter 5\n\n---\n\n## Streaming Optimizations (Perceived Speed is Real Speed)\n\nUsers judge speed by what they see. You can often make the experience feel much faster without changing model tokens/sec.\n\n### 1) Optimize TTFT\n\nTTFT is often dominated by:\n\n- queueing delay\n- long prompt prefill\n\nPractical moves:\n\n- keep interactive queues small (priority tiers)\n- prefill chunking for long prompts\n- keep prompts short and stable\n- cache reusable prefixes (Chapter 4)\n\n### 2) Stream early and consistently\n\nAvoid \"bursty\" streaming where tokens arrive in clumps.\n\nBursty streaming can happen due to:\n\n- buffering in the server\n- network proxies\n- client-side render batching\n\nOperationally, consistent streaming reduces perceived latency and reduces user cancellations.\n\n### 3) Stop early when the user has enough\n\nIf the UI shows:\n\n- a short answer preview\n- a \"continue\" affordance\n\nYou can cap initial output tokens and only generate more on demand. This is a product-level performance optimization.\n\n### 4) Avoid expensive post-processing in the critical path\n\nExample: if you do moderation, logging, reranking, or formatting, do it in a way that does not block streaming unless required.\n\n---\n\n## Failure Modes\n\n### 1) Speculation makes outputs worse\n\nCauses:\n\n- acceptance logic approximations (implementation-dependent)\n- different decoding settings between draft and verifier\n- mismatch in tokenizers or sampling modes\n\nMitigation:\n\n- validate output quality on your real tasks\n- keep decoding settings consistent where possible\n- start with low k and increase only if acceptance is high\n\n### 2) No speedup in your domain\n\nCause:\n\n- draft acceptance rate is low (math, code, or specialized domain)\n\nMitigation:\n\n- choose a better draft model\n- use domain-specific drafting (templates, retrieval spans)\n- invest in batching/kernels first (Ch 5) if speculation is not paying off\n\n### 3) Tail latency gets worse\n\nCause:\n\n- draft model adds extra compute; under load you saturate GPU/CPU differently\n\nMitigation:\n\n- capacity plan for speculation\n- apply it selectively (only for long outputs, or only for certain tiers)\n\n### 4) Debugging is harder\n\nCause:\n\n- two models in the loop, more moving parts, more metrics needed\n\nMitigation:\n\n- log acceptance rate and k usage\n- track verifier tokens/sec and end-to-end latency separately\n\n---\n\n## Deployment Checklist\n\n1. Measure baseline TTFT and decode tokens/sec before adding speculation.\n2. Confirm your server supports draft+verify efficiently (kernel and scheduler support matters).\n3. Choose a draft model with compatible tokenizer and strong domain alignment.\n4. Start with small k; measure acceptance rate and speedup.\n5. Add metrics:\n   - acceptance rate\n   - average accepted tokens per cycle\n   - verifier tokens/sec\n   - end-to-end latency p50/p95/p99\n6. Validate output quality with an evaluation set (not only eyeballing).\n7. Roll out gradually and isolate interactive traffic from batch where possible.\n\n---\n\n## Exercises\n\n1. **Speedup reasoning**: Suppose your draft model is 4x faster than verifier, and average accepted tokens per cycle is 3 when k=5. Explain qualitatively why you might see a large speedup, and list two reasons it might still fail in production.\n2. **Draft model selection**: Propose two candidate draft models for a given verifier model family. What evidence would you gather to pick one?\n3. **Perceived latency**: List three non-model changes you can make that reduce perceived latency in a chat product. For each, name the metric it affects.\n4. **Failure analysis**: Design a minimal dashboard for speculative decoding rollout (metrics and alerts). What would you watch first?\n\n---\n\n## Summary\n\n- Decode is inherently sequential, so speeding it up is hard.\n- **Speculative decoding** uses a fast draft model to propose multiple tokens and a slow verifier to accept many at once.\n- Speedup depends on **draft speed**, **acceptance rate**, and **proposal length k**.\n- Many real wins also come from system-level work: TTFT reduction, consistent streaming, and product-level output limits.\n- Treat speculation as an optimization with measurement and rollback, not as a guaranteed free lunch.",
  },
  {
    slug: ['part-2', 'chapter-7-model-compression'],
    title: 'Chapter 7 - Model Compression',
    description:
      'Quantization, low-precision formats, distillation, and the real-world quality and kernel tradeoffs behind running models cheaply and fast.',
    group: 'Part II - High-Performance Inference',
    toc: [
      'Why Compress',
      'Precision Formats (FP32/FP16/BF16/FP8)',
      'Integer Quantization (INT8/INT4)',
      'Hands-On Walkthrough',
      'Quantization Error Intuition',
      'Post-Training Quantization (PTQ)',
      'GPTQ, AWQ, SmoothQuant (Conceptual)',
      'Quantization-Aware Training (QAT)',
      'Distillation',
      'Serving Implications',
      'Quality and Safety Tradeoffs',
      'Production Failure Modes',
      'Compression Checklist',
      'Exercises',
      'Summary',
    ],
    body: "# Chapter 7 - Model Compression\n\nCompression is how we make models cheaper, faster, and easier to deploy.\n\nBut compression is not just \"make numbers smaller\". In production it is an engineering tradeoff across:\n\n- **memory** (can it fit?)\n- **bandwidth** (how fast can you read weights and KV?)\n- **kernel support** (do you have fast compute for that format?)\n- **quality** (does it still work for your tasks?)\n- **stability** (does it fail in surprising ways?)\n\nThis chapter gives a first-principles explanation of the main compression techniques used in inference:\n\n- low-precision formats (FP16/BF16/FP8)\n- integer quantization (INT8/INT4)\n- post-training quantization methods (GPTQ, AWQ, SmoothQuant conceptually)\n- quantization-aware training (QAT)\n- distillation\n\n---\n\n## Why Compress\n\nCompression is usually motivated by at least one of:\n\n1. **Fit**: the model weights do not fit on your target GPU(s).\n2. **Cost**: you want more throughput per dollar.\n3. **Latency**: you want faster prefill and decode.\n4. **Deployment**: you need to run on consumer GPUs, edge devices, or CPUs.\n\nOne key idea: for large models, inference is often limited by **memory bandwidth** rather than raw compute. If you can read fewer bytes from memory, you can often go faster.\n\n---\n\n## Precision Formats (FP32, FP16, BF16, FP8)\n\n### FP32 (32-bit floating point)\n\n- High precision.\n- Large memory footprint.\n- Rarely used for inference of modern LLMs due to cost.\n\n### FP16 (16-bit floating point)\n\n- Smaller and faster than FP32.\n- Widely supported on GPUs.\n- Can have numerical issues in some training contexts, but inference is usually fine.\n\n### BF16 (bfloat16)\n\n- Same 16-bit size as FP16, but different exponent/mantissa split.\n- Often more numerically stable than FP16 for some operations.\n- Common for inference on modern GPUs.\n\n### FP8\n\n- Even smaller.\n- Can be very fast on supported hardware with strong kernels.\n- Requires careful handling to avoid quality loss (often with scaling).\n\nStudent takeaway:\n\n- Lower precision reduces memory bandwidth and can increase speed, but only if your hardware and kernels support it well.\n\n---\n\n## Integer Quantization (INT8, INT4)\n\nQuantization represents weights (and sometimes activations) with integers plus scaling factors.\n\nConceptually:\n\n\\`\\`\\`text\nfloat weight w  ~  scale * int_weight\n\\`\\`\\`\n\n### INT8\n\n- Often good balance: large memory savings with modest quality impact.\n- Kernel support is widely available in many stacks.\n\n### INT4\n\n- Bigger memory reduction.\n- More likely quality drop if not done carefully.\n- Kernel support and performance vary widely by hardware and library.\n\nImportant nuance:\n\n- compressing weights is not enough; you also need **fast kernels** for the compressed format.\n- a slow INT4 kernel can be worse than a fast BF16 kernel.\n\n---\n\n## Hands-On Walkthrough: Compression Decision to Deployment\n\nThis section is a practical tutorial you can follow conceptually even if your exact tooling differs.\n\n### Step 1: Pick a baseline and define constraints\n\nWrite down:\n\n- target hardware (GPU type, memory size)\n- target workload (prompt/output lengths, concurrency)\n- quality bar (must-pass evaluation tasks)\n\nExample constraint statement:\n\n```text\nTarget: 24 GiB GPU\nWorkload: chat, p95 prompt 1500 tokens, max output 512\nQuality: must not regress tool calling or JSON formatting tests\n```\n\n### Step 2: Budget weights and KV separately\n\nMake a simple budget table:\n\n```text\nGPU memory budget (example):\n  - weights:         12 GiB\n  - KV cache budget:  9 GiB\n  - overhead:         3 GiB (fragmentation, buffers, peaks)\n```\n\nIf you only optimize weights, you can still lose to KV cache (Chapter 4).\n\n### Step 3: Choose a compression tier (pragmatic)\n\n1. BF16/FP16 (baseline)\n2. INT8 weights (common \"safe\" win)\n3. INT4 weights (only after strong validation)\n\n### Step 4: Validate kernel paths (avoid slow fallbacks)\n\nYour benchmark must answer:\n\n- Did I actually get faster prefill?\n- Did I actually get faster decode under concurrency?\n- Did GPU utilization move in the expected direction?\n\nIf your system reports \"INT4\" but throughput did not improve, suspect a slow kernel path.\n\n### Step 5: Quality evaluation (what to include)\n\nInclude tests that are commonly sensitive to compression:\n\n- structured outputs (JSON/schema)\n- tool calling argument accuracy\n- long-context instruction following\n- multilingual prompts if you serve them\n- refusal/safety behavior if relevant\n\n### Step 6: Rollout as a feature flag\n\nIn production, treat compression like a model change:\n\n- shadow traffic or A/B\n- compare quality and latency\n- keep rollback simple\n\n---\n\n## Quantization Error Intuition (First Principles)\n\nQuantization maps a continuous value to a discrete set.\n\nText diagram:\n\n\\`\\`\\`text\nreal line:  ----|----|----|----|----|---->\nlevels:         q0   q1   q2   q3\n\nvalue w gets rounded to nearest q*\nerror = w - q*\n\\`\\`\\`\n\nTwo practical consequences:\n\n1. Small errors accumulate across many layers.\n2. Some weights (or channels) are more \"sensitive\" than others.\n\nThis is why smarter quantization methods try to:\n\n- choose per-channel scales\n- handle outliers\n- preserve important directions in weight space\n\n---\n\n## Post-Training Quantization (PTQ)\n\nPTQ means you take a trained model and quantize it without retraining (or with minimal calibration).\n\nWhy PTQ is popular:\n\n- fast to apply\n- no need for training infrastructure\n\nWhy PTQ can fail:\n\n- some models have outlier weights or activations\n- some tasks (math, code, tool calling) are sensitive to small distribution shifts\n\n### Calibration datasets matter\n\nMany PTQ methods require a representative set of inputs to estimate activation ranges or choose scales.\n\nIf your calibration data does not match your production domain, you can get:\n\n- surprisingly bad behavior on your real workload\n- even if the model looks fine on generic chat prompts\n\n---\n\n## GPTQ, AWQ, SmoothQuant (Conceptual, Student-Friendly)\n\nThere are many named methods; implementations differ, but the conceptual goals are similar.\n\n### GPTQ (conceptual)\n\nGoal:\n\n- quantize weights while minimizing output error, often layer by layer.\n\nIntuition:\n\n- treat quantization as an optimization problem: pick quantized weights that best preserve layer outputs.\n\n### AWQ (conceptual)\n\nGoal:\n\n- improve quantization by paying special attention to \"important\" weight channels.\n\nIntuition:\n\n- some channels contribute disproportionately; preserve them more carefully or scale them.\n\n### SmoothQuant (conceptual)\n\nGoal:\n\n- make activations easier to quantize by \"smoothing\" the distribution via reparameterization.\n\nIntuition:\n\n- if activations have outliers, INT8 quantization gets hard; move some scale into weights so both weights and activations become quantizable with less error.\n\nStudent warning:\n\n- You do not need to memorize method names.\n- You do need to understand the patterns: outliers, per-channel scaling, calibration, and kernel support.\n\n---\n\n## Quantization-Aware Training (QAT)\n\nQAT trains the model while simulating quantization effects.\n\nWhy it helps:\n\n- the model learns to be robust to quantization noise\n\nCosts:\n\n- requires training infrastructure\n- must be done carefully to avoid instability\n\nIn production, QAT is often worth it when:\n\n- you need aggressive quantization (like INT4) but cannot accept quality loss\n- you are deploying at very large scale where efficiency gains justify training cost\n\n---\n\n## Distillation (Compression by Learning a Smaller Model)\n\nDistillation trains a smaller \"student\" model to match the behavior of a larger \"teacher\".\n\nWhy it is powerful:\n\n- you can reduce model size dramatically\n- the student can be architecturally optimized for inference\n\nWhy it is risky:\n\n- the student may lose rare capabilities (long-tail reasoning, domain coverage)\n- it can inherit teacher biases and failures, but also create new ones\n\nDistillation is often paired with quantization:\n\n- distill to a smaller model\n- then quantize the smaller model\n\n---\n\n## Serving Implications (What Changes When You Compress)\n\nCompression changes multiple parts of the inference pipeline:\n\n### Weight memory\n\n- lower precision => less GPU memory used\n- can fit larger models per GPU or replicate more copies for throughput\n\n### Bandwidth and compute\n\n- smaller weights reduce memory bandwidth requirements\n- but speed depends on kernels: you need optimized matmul/attention for the format\n\n### KV cache is separate\n\nImportant: quantizing weights does not automatically quantize KV cache.\n\nSo you can easily end up with:\n\n- smaller weights (good)\n- but KV cache still dominating memory (Chapter 4)\n\nThis is why production capacity planning always includes both weights and KV cache budgets.\n\n---\n\n## Quality and Safety Tradeoffs (What to Test)\n\nCompression can hurt quality in subtle ways:\n\n- increased hallucinations on edge cases\n- worse tool calling reliability\n- more formatting errors (JSON/schema tasks)\n- degraded multilingual performance\n- worse long-context stability\n\nYou should test on:\n\n- your real prompts and domains\n- structured output tasks if you use them\n- safety and policy constraints relevant to your product\n\nDo not rely on a single \"overall score\".\n\n---\n\n## Production Failure Modes\n\n### 1) \"It benchmarks fast but feels worse\"\n\nCause:\n\n- fast kernel for short prompts, but poor behavior under real workload (long context, mixed batching)\n\nMitigation:\n\n- benchmark with your traffic shape: prompt length distribution, output lengths, concurrency\n\n### 2) Silent quality regression\n\nCause:\n\n- small numerical shifts produce different decoding paths\n\nMitigation:\n\n- maintain a regression evaluation set\n- compare quantized vs baseline outputs regularly\n\n### 3) Kernel mismatch (slow path)\n\nCause:\n\n- format is supported but falls back to a slower kernel\n\nMitigation:\n\n- verify actual kernel paths in your stack\n- measure throughput and GPU utilization; do not trust \"it says INT4\" alone\n\n### 4) Tool calling and structured outputs break\n\nCause:\n\n- quantization noise changes token probabilities near constrained boundaries\n\nMitigation:\n\n- use constrained decoding / schema validation\n- consider less aggressive quantization for critical workflows\n\n---\n\n## Compression Checklist\n\n1. Decide your primary goal: fit, cost, latency, or deployment target.\n2. Measure baseline performance and quality on real workloads.\n3. Choose a compression approach:\n   - BF16/FP16 baseline\n   - FP8 if hardware/kernels support it well\n   - INT8 for a common \"safe\" win\n   - INT4 only with careful validation\n4. Verify kernel support and that you are not on a slow fallback path.\n5. Evaluate quality on:\n   - domain prompts\n   - structured outputs/tool calls\n   - long-context tasks\n6. Recompute capacity plan including KV cache (Chapter 4).\n7. Roll out gradually with monitoring and rollback.\n\n---\n\n## Exercises\n\n1. **Bandwidth reasoning**: Explain why reducing weight precision can improve speed even if compute stays the same.\n2. **KV vs weights**: Suppose quantization cuts weight memory in half. In what scenarios does that not help much in production? Use KV cache reasoning to justify.\n3. **Evaluation design**: Create a small evaluation suite for your product that would catch the most likely compression regressions.\n4. **Choosing INT4**: Write a short argument for when INT4 is worth it and when it is irresponsible to deploy.\n\n---\n\n## Summary\n\n- Compression is a tradeoff across memory, bandwidth, kernel support, and quality.\n- Lower precision formats (FP16/BF16/FP8) reduce memory and can improve speed when kernels support them.\n- Integer quantization (INT8/INT4) can dramatically reduce weight size, but quality and kernel support vary.\n- PTQ is fast but calibration-sensitive; QAT is expensive but can preserve quality under aggressive quantization.\n- Distillation makes a smaller model that can be faster and cheaper, often combined with quantization.\n- Production success requires measuring real workloads, verifying kernel paths, and budgeting KV cache separately from weights.",
  },
];
